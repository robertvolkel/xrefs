# XRefs App

An AI-powered component intelligence platform for the electronics industry. Users enter a part number (or upload a BOM), and the system finds equivalent replacements, scores them with a deterministic rule engine, and adapts its recommendations based on the user's role, objectives, and business constraints.

**Current MVP:** Cross-reference recommendation engine covering 43 passive, discrete semiconductor, IC, frequency control, optoelectronic, and relay families, with Digikey as the primary data source.

**Vision:** A platform that helps engineers, buyers, and supply chain professionals make better component decisions by combining deep technical matching with commercial intelligence, compliance and lifecycle awareness, and supply chain insights — personalized to each user's context. See `docs/PRODUCT_ROADMAP.md` for the full vision and phased implementation plan.

## Tech Stack

- **Next.js 16** (App Router, Turbopack) + React 19 + TypeScript
- **MUI v7** with Material Design 3 dark mode (`colorSchemes` API, `enableCssLayer: false`)
- **Anthropic Claude API** (Sonnet 4.6 default, overridable via `ANTHROPIC_MODEL`) for LLM orchestrator with tool calling
- **Supabase** for auth, user profiles, conversation persistence
- **Digikey Product Information API v4** for live part data (OAuth2 client credentials)
- **Emotion** for CSS-in-JS

## Project Structure

```
app/                          # Next.js App Router
  api/search/                 # Part search (Digikey → mock fallback)
  api/attributes/[mpn]/      # Part parametric attributes
  api/xref/[mpn]/            # Cross-reference recommendations (GET + POST with overrides)
  api/chat/                   # LLM orchestrator conversation
  api/modal-chat/             # Refinement chat (per-part in parts list modal)
  api/parts-list/validate/    # Batch validation (streaming NDJSON, direct getAttributes fallback on search miss)
  api/parts-list/search-quick/ # Lightweight MPN search for Add Part dialog (search only, no attrs/recs)
  api/auth/register/          # User registration with invite code
  api/admin/users/            # Admin user management (role changes owner-only)
  api/admin/qc/               # QC log list (GET with filters, sort, pagination)
  api/admin/qc/[logId]/      # QC log detail (GET snapshot)
  api/admin/qc/export/       # QC log export (GET, CSV/JSON download)
  api/admin/qc/analyze/      # QC AI analysis (POST, SSE streaming via Claude)
  api/admin/qc/feedback/     # QC feedback list (GET with status/search/sort)
  api/admin/qc/feedback/[feedbackId]/ # Feedback status update (PATCH)
  api/admin/qc/settings/     # QC settings (GET/PUT logging toggle)
  api/admin/distributor-clicks/ # Distributor click log (GET with filters/search/sort/pagination)
  api/admin/data-sources/    # Data source status (Digikey, Anthropic, Supabase) + cache stats
  api/admin/cache/           # Cache management (GET stats, DELETE purge)
  api/admin/atlas/           # Atlas manufacturer stats (GET, paginated aggregation + coverage % + enabled state)
  api/admin/atlas/manufacturers/ # Atlas manufacturer enable/disable toggle (PATCH, admin-only)
  api/admin/atlas/coverage/  # Atlas per-family attribute gap analysis (GET)
  api/admin/atlas/explorer/  # Atlas product search (GET ?q=, MPN/manufacturer ilike)
  api/admin/atlas/explorer/[id]/ # Atlas product detail + schema comparison (GET)
  api/admin/atlas/dictionaries/ # Atlas dictionary override CRUD (GET list + samples, POST create, supports L2 ?category=)
  api/admin/atlas/dictionaries/suggest/ # AI-assisted Chinese→English param mapping suggestion (POST, Claude Haiku)
  api/admin/atlas/dictionaries/batch/ # Batch-accept N Triage overrides in one request (POST, dedupe + per-family write + ONE invalidation, Decision #267)
  api/admin/atlas/dictionaries/batch/undo/ # One-click Undo of a just-completed Batch Accept (POST, deactivate by PK ids)
  api/admin/atlas/dictionaries/[overrideId]/ # Dictionary override update/delete (PATCH, DELETE)
  api/admin/atlas/param-decisions/ # Decision Log list (GET, newest-first server-sorted, filters + batchCounts; ?include_evidence=1 for AI blobs)
  api/admin/atlas/param-decisions/undo/ # Undo decisions by decision id (POST; appends the reversal, never edits)
  api/admin/atlas/family-schema/ # Canonical attributeId list per family (GET, no Anthropic call)
  api/admin/atlas/ingest/upload/ # Multipart folder upload → data/atlas/, returns staged files + new MFRs (Decision #174)
  api/admin/atlas/ingest/report/ # Generate per-MFR diff report → atlas_ingest_batches (POST)
  api/admin/atlas/ingest/batches/ # List pending/applied batches (GET)
  api/admin/atlas/ingest/batches/[batchId]/ # Batch detail (GET) + discard (DELETE)
  api/admin/atlas/ingest/batches/[batchId]/proceed/ # Apply batch + invalidate admin caches (POST)
  api/admin/atlas/ingest/batches/[batchId]/revert/ # Restore from atlas_products_snapshots (POST, 30d window)
  api/admin/atlas/ingest/batches/[batchId]/regenerate/ # Re-run report after dict overrides change (POST)
  api/admin/atlas/ingest/proceed-all-clean/ # Bulk-apply every risk='clean' batch (POST, concurrency-limited)
  api/admin/atlas/family-param-signatures/ # POST signature + retroactive reclassify RPC in one call (Decision #188); rejects invalid familyIds (Decision #190)
  api/admin/taxonomy/        # Digikey category taxonomy with coverage
  api/admin/overrides/rules/ # Rule override CRUD (GET list w/ admin names, POST create w/ previous_values)
  api/admin/overrides/rules/[overrideId]/ # Rule override update/delete (PATCH deactivate-and-create, DELETE soft)
  api/admin/overrides/rules/history/ # Rule override audit trail (GET full chain for family+attribute)
  api/admin/overrides/rules/restore/ # Restore rule override to previous version (POST)
  api/admin/overrides/rules/annotations/ # Rule annotations (GET family/rule, POST create)
  api/admin/overrides/rules/annotations/[annotationId]/ # Annotation update/delete (PATCH, DELETE own)
  api/admin/overrides/context/ # Context override CRUD (GET list, POST create)
  api/admin/overrides/context/[overrideId]/ # Context override update/delete (PATCH, DELETE)
  api/admin/releases/        # Create release note (POST, admin-only)
  api/admin/releases/[id]/   # Update/delete release note (PATCH/DELETE, admin-only)
  api/releases/              # List release notes (GET, all authenticated)
  api/mouser/enrich/         # On-demand Mouser enrichment (POST, batch MPNs)
  api/list-chat/             # List agent conversation (POST, list-scoped LLM with 9 tools)
  api/maintenance/status/    # Maintenance flag (GET, public) + auto-recovery Claude ping (Decision #273)
  api/admin/manufacturers/    # Manufacturer list endpoint (GET, RPC-based aggregation, 30min cache)
  api/admin/manufacturers/[slug]/ # Manufacturer detail + update (GET/PATCH)
  api/admin/manufacturers/[slug]/products/ # Paginated products for MFR (GET)
  api/admin/manufacturers/[slug]/cross-references/ # MFR cross-reference CRUD (GET list, POST bulk upload, DELETE soft)
  api/admin/atlas/flags/      # Atlas product flags CRUD (GET/POST)
  api/admin/atlas/flags/[flagId]/ # Flag status update (PATCH)
  api/feedback/              # QC rule feedback submission (POST)
  api/app-feedback/          # General app feedback — POST submit + GET own list + DELETE thread + nested GET thread/POST comments (Decisions #162/#220/#222)
  api/admin/app-feedback/    # Admin list + per-row PATCH (status) + nested GET thread (Decisions #220/#222)
  admin/layout.tsx            # Shared admin layout (sidebar persists across routes)
  admin/manufacturers/[slug]/ # Manufacturer detail page (sub-route)
  admin/atlas/ingest/         # Atlas re-ingest UI: drag-drop, batch review, AI dict triage (Decision #174)
  lists/                      # Lists dashboard page
  parts-list/                 # Parts list editor page
  logic/                      # Admin logic table viewer page
  releases/                   # Release notes feed page (all users)
  feedback/                   # User feedback page — own submissions + Trello-style overlay thread (Decision #222)
  qc/                          # QC top-level page (admin-only)

components/                   # React components
  AppShell.tsx                # Main layout orchestrator — composes hooks, delegates to DesktopLayout
  DesktopLayout.tsx           # Desktop grid: chat, attributes, recommendations, MFR panels
  ParticleWaveBackground.tsx  # Canvas particle wave animation — idle-state background, fades on panel reveal
  ChatInterface.tsx           # Chat UI with message list + search input
  AttributesPanel.tsx         # Part attributes table
  RecommendationsPanel.tsx    # Scored replacement list
  ComparisonView.tsx          # Side-by-side source vs replacement
  parts-list/                 # Batch parts list components
    PartsListShell.tsx        # Parts list orchestrator — composes hooks, sort/filter pipeline
    PartsListTable.tsx        # Sortable table with validation progress + selection
    ViewControls.tsx          # View dropdown, kebab menu, default star, delete confirm
    PartsListActionBar.tsx    # Selection count, refresh/delete/add-part buttons, search field
    AddPartDialog.tsx         # Manual part entry — Search → Select picker flow, MPN search with multi-source results
    ListAgentFooter.tsx       # Sticky bottom bar (34px) with toggle trigger + item count + refresh time
    ListAgentDrawer.tsx       # Bottom-anchored 50vh drawer with chat messages + input
    ListActionConfirmation.tsx # Confirm/Cancel widget for write actions (delete, refresh, set preferred)
  lists/                      # Lists dashboard components
  admin/                      # Admin panel components
    AdminShell.tsx            # Admin orchestrator — section selection, family picker state
    AdminSectionNav.tsx       # Left nav for admin sections
    FamilyPicker.tsx          # Shared category/family dropdown (used by 3 sections)
    QcFeedbackTab.tsx         # Feedback list — status filters, search, sort, click-to-detail
    QcFeedbackDetailView.tsx  # Feedback detail — comparison table reconstruction from snapshot
    QcLogsTab.tsx             # Log list + detail (existing log review functionality)
    DistributorClicksTab.tsx  # Distributor click log — filter chips, search, sortable table, pagination
    QcFeedbackCard.tsx        # Individual feedback card (used in logs detail + feedback detail)
    QcRecommendationSummary.tsx # Recommendation summary card (used in logs detail)
    QcAnalysisDrawer.tsx      # AI analysis right-side drawer with streaming markdown
    qcConstants.ts            # Shared dot colors + utility functions for QC components
    LogicPanel.tsx            # Logic table rules viewer/editor (per-family, clickable rows)
    RuleOverrideDrawer.tsx    # Right-side drawer for editing rule overrides
    ContextOverrideDrawer.tsx # Right-side drawer for editing context question overrides
    ParamMappingsPanel.tsx    # Attribute-centric param map: Digikey + Parts.io columns, coverage metrics
    AtlasPanel.tsx            # Atlas manufacturer stats — summary + expandable table + coverage % + Explorer search tab
    AtlasCoverageDrawer.tsx   # Per-family attribute gap analysis drawer (Atlas vs Digikey vs logic table)
    AtlasExplorerTab.tsx      # Atlas product search — debounced MPN/MFR search, results table, opens drawer
    AtlasExplorerDrawer.tsx   # Atlas product detail — schema comparison (L3 or L2), extra attrs, raw params
    AtlasDictionaryPanel.tsx  # Atlas translation dictionary viewer/editor (per-family + L2 category + shared)
    AtlasDecisionLogPanel.tsx # Decision Log — every Triage param decision, newest first, Undo single/bulk, per-param history. REPLACED AtlasAiLogPanel (Decision #277)
    AtlasDictOverrideDrawer.tsx # Right-side drawer for editing dictionary overrides — schema Autocomplete, AI suggestion, sample values
    atlasIngest/              # Re-ingest UI components (Decision #174)
      AtlasIngestPanel.tsx    # Top-level orchestrator (Pending/Applied tabs, dashboard, batch list)
      IngestUploader.tsx      # Drag-drop upload zone + new-MFR confirm step
      IngestDashboard.tsx     # Sticky aggregate header: risk chips + Proceed All Clean / Discard All
      GlobalUnmappedParamsTable.tsx # AI dict triage: per-param suggestions with canonical-vs-invented indicators, bulk-accept. Contains the memoized `TriageRow` — see Decision #269 before editing it
      BatchCard.tsx           # Per-batch card with embedded triage panel when single-batch
      ProductDiffTable.tsx    # Per-product attribute diff inside expanded attention cards
      BatchProgressDialog.tsx # Modal during bulk/single apply
      types.ts                # Shared client-side types for the ingest UI
    ManufacturersPanel.tsx    # Manufacturer list + search + flagged tabs
    ManufacturerDetailPage.tsx # MFR detail (5 tabs: Products, Flagged, Coverage, Cross-Refs, Profile)
    FlaggedProductsTab.tsx    # Flagged products list with status filters
    CrossReferencesTab.tsx    # MFR cross-ref upload (drag-drop + column mapping) + paginated table
    CrossRefColumnMappingDialog.tsx # Column mapping dialog for cross-ref upload (6 fields)
    SearchLogicPanel.tsx      # Admin docs: how single-part search pipeline works (hardcoded markdown)
    ListLogicPanel.tsx        # Admin docs: how batch list validation pipeline works (hardcoded markdown)
    logicConstants.ts         # Shared typeColors/typeLabels for rule type chips
  auth/                        # Registration and onboarding
    RegisterForm.tsx          # Step 1 — account creation (name, email, password, invite code)
    RegisterFlow.tsx          # Two-step wizard orchestrator (registration → onboarding)
    OnboardingAgent.tsx       # Step 2 — conversational state machine (6 guided Q&A + free-form)
  settings/                    # User settings panels
    SettingsShell.tsx         # Settings orchestrator — section nav + content routing
    SettingsSectionNav.tsx    # Left nav: My Account, My Profile, Company Settings, General Settings
    ProfilePanel.tsx          # My Account — name, email, password
    MyProfilePanel.tsx        # My Profile — free-form profile prompt (LLM extraction on save)
    CompanySettingsPanel.tsx  # Company Settings — manufacturers, compliance, country-based locations
    AccountPanel.tsx          # General Settings — language, currency, theme
  releases/                    # Release notes feed
    ReleasesShell.tsx         # Feed UI — create/edit/delete (admin), read-only (users)
  feedback/                    # Collaborative app feedback (Decisions #220/#222)
    FeedbackShell.tsx         # Page orchestrator — full-width card list, modal trigger, 30s polling, delete handler
    FeedbackList.tsx          # Card-style list — activity-first sort, hasUnread border, NEW REPLY label, kebab top-right
    FeedbackThread.tsx        # Reused by user + admin — composer at top, comments newest-first, feed layout (avatar + name + body)
    FeedbackDetailModal.tsx   # Trello-style overlay (60% left details, 40% right thread); viewerRole drives admin status editor
    FeedbackRowMenu.tsx       # Kebab + Delete-thread confirm dialog (both sides; calls DELETE /api/app-feedback/[id])
  qc/                         # QC top-level shell (admin-only)
    QcShell.tsx               # QC orchestrator — settings toggle, section nav, content
    QcSectionNav.tsx          # Left nav: Feedback + Logs sections

hooks/
  useAppState.ts              # Main state machine (LLM mode with deterministic fallback)
  useConversationPersistence.ts # URL hydration, auto-save, conversation CRUD
  usePanelVisibility.ts       # Panel show/hide/dismiss, skeleton delay
  useManufacturerProfile.ts   # MFR panel + chat collapse state — async fetch via /api/manufacturer-profile (Decision #161)
  useNewListWorkflow.ts       # File upload dialog flow
  usePartsListState.ts        # Parts list file upload, parsing, validation
  usePartsListAutoLoad.ts     # Auto-load from URL/pending file, redirect, default view
  useRowSelection.ts          # Multi-select rows, toggle, refresh selected
  useRowDeletion.ts           # Two-step delete confirmation (permanent vs. hide)
  useColumnCatalog.ts         # Header inference, column building, mapping fallback
  useModalChat.ts             # Refinement chat state
  useListAgent.ts             # List agent conversation, action dispatch, confirmation flow
  useViewConfig.ts            # Global view template management (renamed to useViewTemplates)
  useListViewConfig.ts        # Per-list view management (Supabase-backed, debounced save)

lib/
  types.ts                    # All TypeScript interfaces (single source of truth)
  constants/
    profileOptions.ts         # Shared option arrays, curated countries, label lookups (onboarding + settings)
  services/                   # Server-side services (see below)
  services/providers/         # Data-source connector abstraction (Decision #275) — capability-based providers insulate the engine from Digikey
    types.ts                  # Capability-based interface family (data-access ONLY; providers set dataSource, never mfrOrigin)
    digikeyProvider.ts        # CatalogProvider + ParametricCapability
    atlasProvider.ts          # CatalogProvider (DB-only, no facets)
    partsioProvider.ts        # EnrichmentProvider (gap-fill + equivalents)
    findchipsProvider.ts      # CommercialProvider (price/stock/lifecycle/compliance)
    providerRegistry.ts       # Single control point — priority-ordered, config/flag-filtered accessors
    flags.ts                  # Per-phase adoption flags (PROVIDERS_ATTRS/ENRICH/SEARCH/RECS) — all OFF in production
  services/profileExtractor.ts # LLM extraction of structured fields from free-form profile prompt
  logicTables/                # Per-family matching rules (see below)
    e1Optocouplers.ts          # E1 optocoupler logic table
    f1Relays.ts                # F1 electromechanical relay logic table
    f2SolidStateRelays.ts      # F2 solid state relay logic table
  contextQuestions/            # Per-family application context questions
    e1Optocouplers.ts          # E1 optocoupler context questions
    f1Relays.ts                # F1 electromechanical relay context questions
    f2SolidStateRelays.ts      # F2 solid state relay context questions
  supabase/                   # Auth guard, client, server, middleware
  mockData.ts                 # Fallback data (6 MLCCs, 3 resistors, 5 ICs)
  api.ts                      # Client-side API wrapper (includes admin feedback/QC functions)
  services/partsioClient.ts    # Parts.io API client — best-record selection, caching, retry
  services/partsioMapper.ts     # Parts.io listing → ParametricAttribute[] conversion
  services/partsioParamMap.ts   # Maps parts.io field names → internal attributeId (17 class maps)
  services/recommendationLogger.ts # Logs recommendations to Supabase with JSONB snapshots
  services/qcAnalyzer.ts      # Server-side aggregation of QC log snapshots for AI analysis
  services/overrideMerger.ts  # Fetches admin overrides from Supabase, merges onto TS base
  services/overrideValidator.ts # Validates override values against type constraints
  services/overrideHistoryHelper.ts # Audit trail: previous_values snapshots + admin name resolution
  services/atlasClient.ts     # Atlas Supabase queries — search, attributes, candidate fetch
  services/atlasApiClient.ts  # Atlas external API client — manufacturer profile sync (server-side only)
  services/atlasMapper.ts     # Atlas JSON → internal ParametricAttribute[] conversion (28 L3 family + 14 L2 category dictionaries). Holds `storeRawValue`/`rawIdForParam` — the Decision #278 no-value-is-ever-discarded rule, MIRRORED in atlas-ingest.mjs
  services/triageQueueCompute.ts # Heavy Triage aggregation (computeTriageAggregation) + types/predicates; self-registers with triageQueueCache so ANY importer can rebuild a cold cache (Decision #224)
  services/paramDecisionLog.ts # THE single writer of Triage param decisions (Decision #277) — recordParamDecision(s) + decisionForNoteWrite. Append-only; add a route that decides something and you MUST call this (guard test enumerates the six)
  services/paramDecisionBackfill.ts # Pure, unit-tested reconstruction of the decision log from existing records (edit-vs-revoke split, revoke dating)
  services/atlasGaiaDictionaries.ts # Gaia datasheet-extracted param mapping (parse, skip stems, dict exports)
  services/atlas-gaia-dicts.json # Shared gaia dictionaries (12 families + shared, consumed by TS + MJS)
  services/atlasDictOverrides.ts # Server-only Supabase fetch/cache for dictionary overrides
  services/descriptionExtractor.ts # LLM description extraction — schema prompt builder, quote-grounded parser, gap-fill merger
  services/selectionDoc.ts    # Parser/validator/serializer for docs/min_attr_sets.md (Decision #270). Completeness check + cross-family contradiction detector
  services/selectionTiers.generated.ts # GENERATED from docs/min_attr_sets.md by `npm run selection:audit` — never hand-edit
  services/mouserClient.ts    # Mouser API client — search, batch, cache, rate limiter
  services/mouserMapper.ts    # Mouser response → SupplierQuote/LifecycleInfo/ComplianceData
  services/manufacturerCrossRefService.ts # MFR cross-ref lookup (60s cached Supabase query, bidirectional)
  services/mpnNormalizer.ts   # Packaging-suffix stripping for cross-ref lookups (-TR, -REEL, /R7, etc.)
  services/partDataCache.ts   # L2 persistent cache (Supabase-backed, 3-tier TTL)
  columnDefinitions.ts        # Dynamic column system for parts list table
  viewConfigStorage.ts        # View types (SavedView, ViewState), localStorage persistence, sanitizeTemplateColumns()
  layoutConstants.ts          # Shared CSS values (heights, font sizes, spacing)

mcp-server/                   # MCP server for external AI agent integration (Decision #80)
  index.ts                    # Entry point: McpServer + tool registration + StdioServerTransport
  tools/                      # Tool handlers (searchParts, getPartAttributes, findReplacements, etc.)
  lib/envLoader.ts            # .env.local parser for standalone mode
  tsconfig.json               # Standalone TS config (Node16 module resolution)

__tests__/services/           # Jest unit tests for core business logic
docs/                         # Cross-reference logic documents (.docx)
scripts/                      # Utility scripts (Digikey param discovery, Supabase schema, Atlas ingest/extract)
  supabase-atlas-manufacturers-schema.sql # atlas_manufacturers table schema
  supabase-atlas-flags-schema.sql # atlas_product_flags table schema
  atlas-manufacturers-import.mjs # Excel import for manufacturer master list
  atlas-api-validate-ids.mjs  # Validate API partner IDs vs local atlas_id
  atlas-api-sync-profiles.mjs # Sync manufacturer profiles from Atlas external API
  atlas-audit-domain-cards.mjs # Auto-audit family domain cards for hallucinations (Decision #195)
  atlas-revoke-bad-canonical.mjs # Audit + soft-revoke a hallucinated attributeId from atlas_dictionary_overrides; dry-run by default (Decision #215)
  atlas-audit-unit-mismatches.mjs # Enumerate every (attributeId, unit) combo in dict + DB overrides; 3-tier risk report — read-only (Decision #217)
  atlas-find-mfrs-needing-backfill.mjs # Scan atlas_products for prefix-triggering units; priority-ordered per-MFR punch list with copy-paste backfill commands (Decision #217)
  atlas-mark-condition-params-unmappable.mjs # Bulk-mark vendor test-condition columns (Galaxy ConditionN_*) unmappable in Triage; dry-run by default, --apply to write, idempotent
  atlas-audit-b7-negative-ic.mjs # Read-only audit: B7/IGBT products carrying negative collector-current (PNP-BJT misclassification signal); rolls up by MFR
  atlas-dedupe-manufacturers.mjs # Collapse TRUE-duplicate atlas_manufacturers rows (same name_en+name_zh = one company imported twice); dry-run default, --apply, snapshot-before-delete; keeper=richest row, ties abort (Decision #225)
  atlas-rekey-collision-products.mjs # Fix manufacturer-collision mis-attribution (HX=红星/恒佳兴): re-key colliding products' manufacturer to the full source name via atlas_source_file; dry-run default, --apply, snapshot (Decision #225)
locales/                      # i18n translations (en, de, zh-CN)
theme/theme.ts                # MUI M3 dark theme configuration
jest.config.ts                # Jest config (next/jest.js, SWC transforms, path aliases)
```

## How the Matching Engine Works

The core pipeline is in `lib/services/partDataService.ts → getRecommendations()`:

1. **Get source attributes** — Fetch the original part's parametric data (Digikey API, mock fallback)
2. **Merge overrides** — Apply any user-supplied attribute corrections
3. **Classify family** — Map subcategory string → family ID, detect variant families (e.g., current sense within chip resistors) via `lib/logicTables/familyClassifier.ts`
4. **Get logic table** — Load the family's matching rules from `lib/logicTables/`
5. **Apply admin overrides** — `overrideMerger.ts` fetches active DB overrides and merges onto the TS base (remove→override→add pattern). Patchable fields include `valueAliases` (Decision #160) for per-rule categorical synonym groups.
6. **Apply context** — If user answered application context questions, `contextModifier.ts` adjusts rule weights/types. Context questions are also subject to admin overrides.
7. **Fetch candidates** — Search Digikey for replacement candidates using critical parameters as keywords
8. **Score candidates** — `matchingEngine.ts` evaluates each candidate against every rule
9. **Apply qualification-domain filter** (Decisions #155, #196, broadened by #211) — After scoring, classify source + every candidate into a `QualificationDomain` (automotive_q200 / automotive_q101 / automotive_q100 / medical_implant / mil_spec / space / ...) via `qualificationDomain.ts`. Hard-exclude cross-domain candidates (medical_*/mil_spec/space under automotive) **before** the certified-cross bypass so certified crosses can't route around the gate. Phase 1.5 (#196): also hard-excludes `commercial` and `industrial_harsh` candidates under automotive intent. `unknown` candidates stay visible — most Atlas products and non-classified MFRs lack the AEC attribute even when AEC-qualified, so excluding `unknown` would wipe out legitimately-qualified parts. Classifier registry still Phase 1 (Murata MLCCs only) — expanding to TDK / Samsung / Yageo / KEMET / AVX / Kyocera / Taiyo Yuden tracked in BACKLOG. **Attribute-based upgrade** (Decision #211): `upgradeFromAttributes` now checks `aec_q200`/`aec_q101`/`aec_q100` parametric attributes AND the `part.qualifications[]` string array (populated uniformly by `extractQualifications` across all Digikey categories — load-bearing for C1 LDOs / C6 Voltage Refs and other categories where the per-category param map doesn't carry AEC fields).
10. **Apply post-scoring family filters** — `withCertifiedBypass` applies per-family blocking-rule filters (C2/C4–C10/D1–D2/E1/F1–F2); bypassed for MFR-certified and Accuris-certified crosses (Decision #133). **Automotive AEC enforcement is now table-driven** (Decision #221, full rollout June 3, 2026): `AUTOMOTIVE_AEC_ENFORCEMENT` table in `partDataService.ts` enrolls all 11 AEC-aware families (B5/B6/B7/B8/B9 AEC-Q101, C9/C10 AEC-Q100, D1/D2/F1 AEC-Q200, E1 AEC-Q101). Each row carries `{ familyId, questionId, answerValue, attributeId }` because family files don't agree on a single context questionId — B-block + C-block use `automotive`, D1 uses `extended_temp_automotive`, D2/F1 use `automotive_aec_q200`, E1 uses `automotive_aec_q101`. One generic `filterAutomotiveAecMismatches(recs, src, ctx, familyId)` consumes the table; one generic `applyContextSourceOverrides` iterates the table and synthesizes `aec_q*: 'Yes'` onto the scoring-local source so the identity_flag rule fires. E1 and F1 chain — the new AEC filter runs alongside the existing `filterOptocouplerMismatches` / `filterRelayMismatches` (independent concerns, order-independent). Adding a new family is a one-row addition; no new code paths.
11. **Sort** — `sortRecommendationsForDisplay` sorts by **Active-first (top-level key, Decision #244)**, then certification bucket (Accuris → MFR → Logic) within the lifecycle tier, then mfr-equivalence rank, qualification domain (`context-matched > unknown > deviation`), then **match % / composite score**. Decision #244 made Active the top key (a dead part rarely belongs above a live one — fixes obsolete crosses topping the panel) and replaced the Decision #236 raw-fail-count primary with match %, which is data-aware: a missing attribute scores `review` = 50% credit (vs `fail` = 0), so a sparse candidate is dragged toward 50% and can't masquerade as a flawless 0-fail match the way `countRealMismatches` let it (it ignored `review` entirely). Raw fail-count survives only as a display filter (`filterRecsByMismatchCount`), never as a sort key.

### Scoring

Each rule has a **weight** (0-10). The matching engine evaluates each rule and produces:
- `matchPercentage = (earnedWeight / totalWeight) * 100`
- A part **fails** if any rule result is `'fail'` (actual value mismatch only — missing data never causes fail)
- Missing candidate data → `'review'` (flagged for human verification, never rejected). `blockOnMissing` flag controls note severity only, not the result (Decision #109).
- `application_review` rules get 50% credit (can't be automated)
- `operational` mismatches get 80% credit (non-electrical)

### Rule Types

Defined in `lib/types.ts` as `LogicType`:

| Type | Behavior | Example |
|------|----------|---------|
| `identity` | Exact match required — string-equality-first check (after normalize), then per-rule `valueAliases` lookup (Decision #160), then numeric fallback with 1e-6 relative tolerance for SI-prefix encoding drift; supports optional `tolerancePercent` band. Decision #137. | Capacitance, package/case, fsw ±10%, Polar≡Polarized |
| `identity_range` | Range overlap required (replacement range must intersect source range) | JFET Vp, Idss |
| `identity_upgrade` | Match or superior per hierarchy (best→worst array); also consults `valueAliases` so synonyms map to hierarchy positions (Decision #160). | Dielectric: C0G≡NP0 > X7R > X5R |
| `identity_flag` | Boolean gate — if original requires it, replacement must too | AEC-Q200, flexible termination |
| `threshold` | Numeric comparison: `gte`, `lte`, or `range_superset` | Voltage ≥, ESR ≤, temp range ⊇ |
| `fit` | Physical constraint ≤ | Component height |
| `vref_check` | Cross-attribute Vref → Vout recalculation with ±2% tolerance | Switching regulator Vref |
| `application_review` | Cannot be automated, flagged for human review | DC bias derating |
| `operational` | Non-electrical info | Packaging type |

## Logic Tables & Family Structure

Each component family has a logic table in `lib/logicTables/` defining its matching rules. All 43 families are encoded:

**Registry:** `lib/logicTables/index.ts` maps family IDs to tables and subcategory strings to family IDs.

| ID | Family | Category |
|----|--------|----------|
| 12 | MLCC Capacitors | Passives |
| 13 | Mica Capacitors | Passives |
| 52 | Chip Resistors | Passives |
| 53 | Through-Hole Resistors | Passives |
| 54 | Current Sense Resistors | Passives |
| 55 | Chassis Mount Resistors | Passives |
| 58 | Aluminum Electrolytic | Passives |
| 59 | Tantalum Capacitors | Passives |
| 60 | Aluminum Polymer | Passives |
| 61 | Supercapacitors | Passives |
| 64 | Film Capacitors | Passives |
| 65 | Varistors / MOVs | Passives |
| 66 | PTC Resettable Fuses | Passives |
| 67 | NTC Thermistors | Passives |
| 68 | PTC Thermistors | Passives |
| 69 | Common Mode Chokes | Passives |
| 70 | Ferrite Beads | Passives |
| 71 | Power Inductors | Passives |
| 72 | RF/Signal Inductors | Passives |
| B1 | Rectifier Diodes | Discrete Semiconductors |
| B2 | Schottky Barrier Diodes | Discrete Semiconductors |
| B3 | Zener / Voltage Reference Diodes | Discrete Semiconductors |
| B4 | TVS Diodes — Transient Voltage Suppressors | Discrete Semiconductors |
| B5 | MOSFETs — N-Channel & P-Channel | Discrete Semiconductors |
| B6 | BJTs — NPN & PNP | Discrete Semiconductors |
| B7 | IGBTs — Insulated Gate Bipolar Transistors | Discrete Semiconductors |
| B8 | Thyristors / TRIACs / SCRs | Discrete Semiconductors |
| B9 | JFETs — Junction Field-Effect Transistors | Discrete Semiconductors |
| C1 | Linear Voltage Regulators (LDOs) | Voltage Regulators |
| C2 | Switching Regulators (DC-DC Converters & Controllers) | Voltage Regulators |
| C3 | Gate Drivers (MOSFET / IGBT / SiC / GaN) | Gate Drivers |
| C4 | Op-Amps / Comparators / Instrumentation Amplifiers | Amplifiers |
| C5 | Logic ICs — 74-Series Standard Logic | Logic ICs |
| C6 | Voltage References (Series / Shunt / Buried Zener) | Voltage References |
| C7 | Interface ICs (RS-485, CAN, I2C, USB) | Interface ICs |
| C8 | Timers and Oscillators (555, XO, MEMS, TCXO, VCXO, OCXO) | Timers and Oscillators |
| C9 | ADCs — Analog-to-Digital Converters (SAR, Delta-Sigma, Pipeline, Flash) | ADCs |
| C10 | DACs — Digital-to-Analog Converters (Voltage Output, Current Output, Audio) | DACs |
| D1 | Crystals — Quartz Resonators | Crystals |
| D2 | Fuses — Traditional Overcurrent Protection | Fuses |
| E1 | Optocouplers / Photocouplers | Optocouplers |
| F1 | Electromechanical Relays (EMR) | Relays |
| F2 | Solid State Relays (SSR) | Relays |

**Per-family detail** — each family's rules, hard gates, context questions, MPN-enrichment patterns and Digikey parameter coverage (C3–F2 plus the variant families) lives in [docs/FAMILIES.md](docs/FAMILIES.md). **Read the relevant family there before changing its logic table, param map or classifier.**

**Variant families** (53, 54, 55, 60, 13, 72, B2, B3, B4) are derived from base families using `deltaBuilder.ts` — the classifier in `familyClassifier.ts` detects them from part attributes. B2 (Schottky) is classified from B1 (Rectifier Diodes) when the part description contains 'Schottky', 'SBD', or 'SiC diode'. B3 (Zener) is classified from B1 when the description contains 'Zener', 'voltage reference diode', or MPN starts with 'BZX', 'BZT', 'MMSZ'. B4 (TVS) is classified from B1 when the description contains 'TVS', 'transient voltage', 'ESD protection', or MPN matches TVS prefixes (SMAJ, SMBJ, P6KE, PESD, TPD, ESDA, etc.). B5 (MOSFETs) is a standalone base family — detected by subcategory mapping ('MOSFET', 'FET', 'N-ch', 'P-ch', 'SiC MOSFET', 'GaN FET' keywords). B6 (BJTs) is a standalone base family — detected by subcategory mapping ('BJT', 'Bipolar Transistor', 'NPN', 'PNP' keywords). B7 (IGBTs) is a standalone base family — detected by subcategory mapping ('IGBT', 'Insulated Gate Bipolar Transistor' keywords). B8 (Thyristors) is a standalone base family — detected by subcategory mapping ('SCR', 'TRIAC', 'DIAC', 'Thyristor', 'SIDAC' keywords). Three sub-types (SCR/TRIAC/DIAC) share one logic table; context question Q1 suppresses irrelevant rules per sub-type via `not_applicable` effects. B9 (JFETs) is classified as a variant of B5 (MOSFETs) when detected by description keywords ('JFET', 'J-FET', 'junction field effect', 'depletion mode FET') or MPN prefixes (2N54xx, 2SK, 2SJ, J112, J113, MPF102, BF245, IFxxx). Uses the new `identity_range` LogicType for Vp and Idss range overlap matching.

**Context questions** in `lib/contextQuestions/` provide per-family application context that modifies rule weights at evaluation time. The TS source is authoritative for the **matching logic** (engine keys off `questionId` + answer `value` + `attributeEffects`), but the UI renders question **titles and option labels** from the i18n locale files (`contextQ.<familyId>.<questionId>.text`), using the TS `questionText`/`label`/`description` only as the *absent-key fallback* — see [components/ApplicationContextForm.tsx](components/ApplicationContextForm.tsx). So a locale typo can silently misrepresent a question on-screen without affecting scoring (Decision #227 — a broken extractor had clobbered 5 families' Q1 titles with the automotive question's text). Guard test [__tests__/services/contextQuestionTranslations.test.ts](__tests__/services/contextQuestionTranslations.test.ts) pins `en` == TS verbatim + intra-scope label/title uniqueness + no-corruption across all locales. When editing context questions, update the TS source; `en` locale entries are redundant and must stay byte-identical (or be deleted to fall back).

## The docs/ Folder

Contains 31 `.docx` files — one per base component family — defining the cross-reference logic rules that were encoded into the TypeScript logic tables. These are the authoritative source documents. The `passive_variants_delta.docx` covers the 6 variant families.

Also: `application-context-attribute-map.md` — comprehensive guide mapping families to context questions with effects.

See `docs/DECISIONS.md` for architectural decisions and `docs/BACKLOG.md` for known gaps.

## Key Patterns

- **OrchestratorMessage/OrchestratorResponse** types live in `lib/types.ts`, not in the orchestrator module, to avoid client bundles importing `@anthropic-ai/sdk`
- **enableCssLayer must be false** in ThemeRegistry — setting it to true causes MUI styles to lose specificity
- **globals.css** should only have `box-sizing: border-box` on `*` — no `padding: 0; margin: 0`
- **Layout constants** are shared via `lib/layoutConstants.ts` (HEADER_HEIGHT, ROW_FONT_SIZE, etc.)
- **Two-step panel reveal**: idle → 70/30 (attributes) → 40/30/30 (recommendations)
- **Particle wave background**: Canvas animation in `ParticleWaveBackground.tsx` shows in idle state, fades out when attributes panel appears. Grid container is transparent; individual panels have opaque `bgcolor` so they cover the canvas.
- **useAppState** tries Claude API first; on an AI-call failure it falls back to deterministic mode for THAT message and retries the AI on the next turn (per-message, no session-wide latch — Decision #266); with no API key it stays deterministic throughout
- **Automatic maintenance mode** (Decision #273): when Claude credits run out, the chat/modal-chat/list-chat route catch blocks call `maybeEnterMaintenance(err)` — which flips a global flag ONLY on the out-of-credits signature (`isOutOfCreditsError` in `lib/services/maintenanceMode.ts`: 403 `billing_error` or 400/403 "credit balance"; NEVER 429/529/500/network). Regular signed-in users then see a full-screen "robot power-nap" notice (`components/MaintenanceScreen.tsx` via `components/MaintenanceGate.tsx`, mounted in `app/layout.tsx`); admins bypass with a small ribbon; the login screen shows a matching banner but stays usable. **No manual switch** — recovery is automatic: public `GET /api/maintenance/status` fires a DB-throttled (once/min) 1-token Claude ping using the app's own model; success clears the flag. All flag reads/writes go through `SECURITY DEFINER` functions (`scripts/supabase-platform-settings-add-maintenance.sql`) because `platform_settings` RLS would silently no-op a non-admin/anon write.
- **Multi-source search** (Decision #106, updated #135): `searchParts()` queries Digikey + Atlas + Parts.io in parallel. `looksLikeMpn()` heuristic routes MPN-like queries to Parts.io additionally; descriptions only hit Digikey + Atlas. Priority dedup: Digikey wins, non-Digikey results only for parts not in Digikey. Result cap 50. `PartSummary.dataSource` tags origin. Mouser removed from search (Decision #135) — parts discoverable only via Mouser caused dead-end attribute fetches since `getAttributes()` has no Mouser fallback. Mouser retained for `SuggestedReplacement` lookups only (Decision #97).
- **partDataService** tries Digikey first; if unavailable, falls back to parts.io → Atlas → null. After Digikey, enriches with parts.io gap-fill AND Mouser commercial data **in parallel** via `enrichSourceInParallel()` (Digikey values win on conflicts). When parts.io is the primary source (no Digikey/Atlas match), `disambiguatePartsioSubcategory()` refines broad Class names (e.g., "Capacitors") into specific families using description keywords and parametric hints. Batch validation has an additional fallback: if `searchParts()` returns 'none', tries `getAttributes()` directly (exact MPN lookup) before marking "not-found" (Decision #108).
- **Data-source connector abstraction** (Decision #275): a capability-based provider layer (`lib/services/providers/`) insulates the engine from Digikey so a source can be swapped. Each source implements a small typed **data-access-only** interface (`CatalogProvider` / `EnrichmentProvider` / `CommercialProvider` + optional `ParametricCapability` mix-in); providers advertise `ProviderCapabilities` booleans the orchestrator branches on (Digikey alone has `facets`/`parametricFilter`). **Providers set `dataSource` (provenance) and NEVER `mfrOrigin`** (Chinese/Western identity stays in the orchestrator via `resolveManufacturerAlias` — enforced by a test). All scoring/filtering/sorting/merge-order/cache stays in `partDataService`. Adoption was **flag-gated in 6 phases** (`PROVIDERS_ATTRS`/`ENRICH`/`SEARCH`/`RECS`, read at call time; **all OFF in production** so behavior is unchanged), each proven byte-identical flag-off-vs-on via the characterization harness (`scripts/providers-characterize.ts`, oracle logic unit-tested in `__tests__/scripts/characterizationCore.test.ts`). **Turn Digikey off with `DIGIKEY_PROVIDER_DISABLED=1`** — the single gate inside `isDigikeyConfigured()` (`digikeyClient.ts`) that every Digikey path already funnels through (3 entry points + 2 widening islands + provider adapter → registry drops it); no second check to drift. Honest limit: this insulates the *code*, not the *capability* — nothing else supplies live Western parametric facet search, so losing Digikey degrades search/widening even with a perfect abstraction (capability matrix + deferred parametric-widening convergence in [docs/CONNECTOR_ABSTRACTION.md](docs/CONNECTOR_ABSTRACTION.md)).
- **Persistent L2 cache** (Decision #99, modified by #158): `part_data_cache` Supabase table sits between in-memory L1 caches and live API calls. Three-tier TTL: parametric (indefinite Digikey / 90-day parts.io), lifecycle (6 months), commercial (24 hours). Cross-user, survives cold starts. Digikey keyword search results warm the cache via `warmCacheFromSearchResults()`. Not-found results cached 24h. All writes fire-and-forget. Admin: `GET/DELETE /api/admin/cache`, cache stats in data-sources endpoint; per-MPN purge UI on `DataSourcesPanel.tsx`. **FindChips carve-out (Decision #158):** FC pricing/stock are NOT persisted at L2 — `getFindchipsResults()` does not write `fc-results` rows, and L1 in-memory TTL is 5 min (down from 30) so users never see hours-stale numbers. Digikey's commercial slice (`commercial:${currency}` variant) still uses the 24h commercial tier.
- **Search cache source provenance** (Decision #126, updated #128): `SearchResult.sourcesContributed` tracks which data sources were successfully queried (even 0-result queries count). `shouldBypassSearchCache()` only bypasses legacy entries missing `sourcesContributed`. Per-source checks were removed — they caused a sticky bypass when Mouser returned 0 results for obscure MPNs.
- **Recommendations L2 cache** (Decision #128, split by #163): two-tier. **Full-result tier** (`rec:<RECS_CACHE_SCHEMA_VERSION>:<mpn>:<hash>`, 30-day TTL): caches `RecommendationResult` keyed on overrides + context + prefs + currency + options. Hits when every input matches. **Base-payload tier** (`rec-base:<BASE_RECS_SCHEMA_VERSION>:<mpn>:<hash>`, 30-day TTL): caches pre-scoring artifacts (`sourceAttrs`, `allCandidates`, certification/equivalence/dataSource/enrichedFrom maps) keyed only on `mpn + overrides + currency + opts` — context, user prefs, and replacement priorities are NOT in the key. Lets a context-only change skip the heavy fetch block, turning a 3-8s rerun into ~100-300ms in-memory rescore. Both tiers invalidated by `invalidateRecommendationsCache()`. Bump `RECS_CACHE_SCHEMA_VERSION` for full-result schema changes; bump `BASE_RECS_SCHEMA_VERSION` for base-payload shape changes.
- **MFR alias resolver L2 cache** (Decision #163 Fix 1): `manufacturerAliasResolver.ts` adds Supabase-backed L2 over the existing 5-min in-memory cache. Stores raw rows from `atlas_manufacturers` + `manufacturer_companies` + `manufacturer_aliases` under sentinel key (`mpn='__mfr_alias_index__'`, `variant='v1'`), 6-month TTL. Cold-start drops from ~400-600ms to ~30-60ms. `invalidateManufacturerAliasCache()` clears both L1 and L2; called from admin alias-edit routes. Bump `MFR_ALIAS_L2_VERSION` when row shapes change.
- **Deferred parts.io candidate enrichment** (Decision #163 Fix 3): single-part flow fires `/api/xref/[mpn]` POST with `skipPartsioEnrichment: true` for the initial fast call (cards render ~500-1500ms sooner on Digikey-only scoring), then `triggerPartsioEnrichment()` re-fires the same call without the flag in the background. Server hits the base-payload cache to skip candidate re-fetching, runs parts.io gap-fill, re-scores, returns updated recs; client replaces state and re-fires `triggerFCEnrichment()` (FC L1 cache → instant supplier merge). Mirrors the existing FC / LLM deferred-enrichment pattern. Digikey down → enrichment is no-op; parts.io down → background call errors silently, cards stay on Digikey-only scoring.
- **Distributor count on search-result cards** (Decision #164): `PartSummary.distributorCount` populated two ways. (1) **L2 cache**: `getFindchipsResults()` writes `{ count }` under `service='findchips', variant='fc-distributors'`, 6-month TTL — distributor identity is stable on weeks/months timescale unlike pricing/stock. `searchParts()` batch-reads via `getCachedDistributorCounts()` after merge. (2) **Post-search progressive enrichment**: `triggerSearchDistributorEnrichment(messageId, parts)` in `useAppState.ts` mirrors `triggerFCEnrichment` — fires `enrichWithFCBatch` for cold MPNs after cards render, merges counts into both the message's `interactiveElement.parts` and `state.searchResult.matches` (so the LLM sees fresh counts on the next chat turn). Cards show "N distributors" chip when count > 0. Search itself unchanged in latency.
- **Conversational chat over search results** (Decision #165): four routing modes — Refine (subset of current cards), Pivot (new search), Ask (parametric Q), Link (inline MPN clickable). Tools: `present_part_options(mpns)` re-renders a chosen subset of cards (resolves MPNs against `currentSearchResult.matches`, populates `data.searchResult` so the existing reset path collapses right panels for free); `get_batch_attributes(mpns)` (max 10, concurrency 5) returns compact projection per part for compound parametric Qs. `find_replacements` REMOVED from chat tools — cross-refs are strictly button-driven; the LLM tool fired cross-refs when users were just asking parametric questions. `summarizeSearchResults()` injects MPN list + lightweight params into LLM context (mirrors `summarizeRecommendations`). Inline MPN auto-linking in `MessageBubble.tsx`: `knownMpns: Set<string>` (built from `searchResult.matches + allRecommendations + sourcePart` + **tool-resolved parts** per Decision #264) drives a ReactMarkdown `components` override on `p`/`li`/`strong`/`em`/`td` that linkifies known MPNs only — no regex over arbitrary text → zero false positives. `handleMpnClick` resolves MPN → PartSummary → existing `handleConfirmPart`. New `searchResultRef` mirrors `state.searchResult` for async callbacks.
- **Tool-resolved MPNs are clickable in chat answers** (Decision #264): when the assistant answers a parametric question (e.g. a ranked "which has the lowest Vos" table) via `get_batch_attributes` / `present_comparison`, the parts it looks up are NOT in the search cards / recs / source part — so they weren't in `knownMpns` and rendered as plain text. The orchestrator already returns them on `OrchestratorResponse.attributes` (`Record<requestedMPN, PartAttributes>`); the client just never read it. Fix (client-only, mirrors the Decision #203 `chatAtlasMfrs` pattern): `AppState.chatMentionedParts: ReadonlyMap<string, PartAttributes>` accumulates `response.attributes` across the session (keyed by BOTH requested + canonical `part.mpn`, lower-cased — catalogs canonicalize BC847B→BC847BLT1G). `AppShell.knownMpns` unions its keys; `handleMpnClick` gains a 4th fallback that builds a `PartSummary` from the stored attrs and `handleConfirmPart`s with them as the Decision #257 optimistic preview. **Grounded:** only tool-*resolved* (real catalog) parts land here — a fabricated MPN returns `notFound` and stays un-clickable. Covers both the model-written markdown table and the system-built `ComparisonTable` (which gates on `knownMpns.has(row.mpn)`). Zero change to the greenfield search pipeline — `knownMpns`/`handleMpnClick`/`chatMentionedParts` are display-side only.
- **MFR profile tool + general claim discipline** (Decision #166): `get_manufacturer_profile(name)` wraps `getProfileForManufacturer()`. Coverage: Chinese MFRs in Atlas with rich profiles + a few Western majors via mock fallback; everything else returns `{ notFound: true }` and the model is instructed to tell the user plainly we only carry detailed profiles for Chinese MFRs and offer the search-based fallback. Surfaced four additional fields (`stockCode`, `websiteUrl`, `contactInfo`, `partsioName`) through `mapAtlasToManufacturerProfile()` and the tool projection — Atlas DB had them but the canonical type dropped them, leading to the GigaDevice "Private company" failure when `stock_code: "603986"` was right there. `contactInfo` typed as permissive union (`string | Record<string, string>`) for JSONB shape variance. **Architectural note**: the system prompt initially accumulated five+ overlapping rule blocks for cert-audit specifics; refactored into one general claim-discipline rule ("every factual claim must have a backing source in tool data; otherwise downgrade to 'not in our profile' or hedge as interpretation") plus one per-shape template (industry-fitness cert audit with per-industry checklists for automotive / medical / aerospace / space / defense). New question shapes (financial, M&A, EOL, leadership, ESG) ride the general rule for free — no per-shape patches. Bar for adding future templates: question must have (a) implicit completeness requirement + (b) domain-knowledge-derived checklist not derivable from tool data.
- **chat() prompt structure + safe-edit protocol** (Decision #241): the `chat()` `SYSTEM_PROMPT` ([lib/services/llmOrchestrator.ts](lib/services/llmOrchestrator.ts)) is organized into ~15 named sections with the grounding/claim-discipline floor consolidated near the top (Phase 1 reorg, merged `8f20bc2`). There is **no automated test for prompt behavior** — after ANY edit to a chat-orchestrator prompt, re-run the manual behavioral checklist [docs/chat-prompt-regression-checklist.md](docs/chat-prompt-regression-checklist.md) (19 scenarios on Haiku `claude-haiku-4-5`) and diff against the recorded baseline; the acceptance gate is that every CRITICAL+HIGH baseline-PASS row stays PASS. Phases 2-3 (move-to-deterministic, then consolidate the grounding blocks into one free-prose floor) deferred — see #241 + BACKLOG.
- **Guided part selection + deterministic greenfield search presentation** (Decision #242): (1) **Guided selection** — vague part requests ("I need a transistor", "help me pick a capacitor") get ONE consolidated narrowing turn (the `chat()` SYSTEM_PROMPT §"Part-selection-advice discipline" guided-selection bullet), then a search. Prompt-only, no new tool — reuses `present_choices` `action:'other'` (already round-trips a clicked label back as a user turn via `handleChoiceSelect`) + descriptive `searchParts`. Decision boundary **SEARCH-NOW > GUIDE > ANSWER-FIRST** (first-match-wins; the guide branch only owns the residue so it can't poach the tuned act-don't-ask / answer-first behaviors). Single-shot, not multi-turn (minimizes free-prose exposure on Haiku). (2) **Deterministic greenfield presentation** — on greenfield (non-MPN) searches the chat bubble is a deterministic one-liner from `buildSearchSummary()` ([lib/services/searchSummary.ts](lib/services/searchSummary.ts)), NOT LLM prose; kills the A3 fabrication leak (Haiku reciting from-memory parts with wrong specs on broad searches) via the Decision #173 pattern. `handleSearchWithLLM` substitutes it in BOTH the bubble AND the `conversationRef` push (the latter fixes history-poisoning), scoped by `!looksLikeMpn(query)` — so MPN lookups keep the LLM confirmation message (rules C6/B2). `looksLikeMpn` relocated from server-only `partDataService` to client-safe `searchSummary.ts` (re-exported for back-compat). Validated on the Haiku checklist (new section E); retires the A3 leak (the lone exempt #241 baseline FAIL) WITHOUT the risky Phase-3 consolidation. Remaining chat-prompt work (residual free-prose leaks on inherently-prose surfaces A4/A5/C5/E5, dormant excluded-MFR feature, Phases 2-3) is in BACKLOG "Chat-prompt remaining work".
- **Logic-vetted descriptive search** (Decision #243): descriptive (greenfield) searches that carry stated specs now run candidates through the matching engine so off-target parts sink instead of topping the list (a "MOSFET 12V 5A" ask was returning 1700V/1200V parts — keyword search ignores numbers as filters). The `search_parts` tool gained optional `partType` + `constraints[]` (human terms like `{attribute:'drain-source voltage', value:12, unit:'V'}`, emitted by the LLM ONLY on descriptive searches per the "Act, don't ask" prompt bullet). `searchParts` ([partDataService.ts](lib/services/partDataService.ts)) builds a **synthetic source part** from those specs ([searchConstraints.ts](lib/services/searchConstraints.ts) `buildSyntheticSource` — classify family by plurality of candidate subcategories → fallback `resolveFamilyFromText(partType)`; map human term → attributeId via the logic table's own `attributeName` + a B5-thorough synonym map; normalize to base SI), then scores every candidate via the same `findReplacements` engine used for cross-refs and reranks: **fewest real mismatches → Active-first → over-spec closeness → match %** (rank, KEEP ALL — nothing hidden). **Two load-bearing invariants:** (1) a rule whose SOURCE value is missing returns `pass` (evaluateIdentity/evaluateThreshold), which is what lets a sparse synthetic source gate only the constrained attrs without diluting; (2) match % can't sink over-spec (a 30V and a 1700V part both pass `gte` → same %), so `computeOverSpecPenalty` (`log(candidate/required)` over numeric gte constraints) is the closeness tiebreak — applied ONLY in the search-vetting sort, NEVER in shared `sortRecommendationsForDisplay` (a higher-rated cross is genuinely better). **Zero extra API calls** — candidate attrs rebuilt from data already in the payloads (`mapKeywordResponseToAttributesByMpn` for Digikey; `searchAtlasProducts` now returns `{result, attrsByMpn}` from the Atlas rows' JSONB). `PartSummary` gained optional `matchScore`/`failCount`/`hardFail` (additive); [PartOptionsSelector](components/PartOptionsSelector.tsx) shows a qualitative **"Fits your specs"/"Below spec"** chip (NOT a raw % — it's ~100 for every valid part vs a sparse spec); `buildSearchSummary` says "ranked by how well they fit your specs" when vetted; search cache key includes a constraints+partType signature (`SEARCH_CACHE_SCHEMA_VERSION` bumped v2→v3). **No regression:** fires only when constraints present AND family classifiable — else byte-identical to old keyword behavior (MPN lookups / `/api/search` / BOM validation pass no constraints). **Hot-path perf:** candidate-attrs building (digikey `mapKeywordResponseToAttributesByMpn`; atlas `searchAtlasProducts(..., buildAttrs)`) is gated behind constraints-present so plain MPN lookups don't pay for it. Family is classified over the **scored (MFR-filtered) population**, and `resolveFamilyFromText` is whole-word (not substring) so short acronym keys don't misfire inside unrelated words. v1 nuances (dual-channel penalty-0 float; current over-spec penalized like voltage; over-spec sunk not hidden) are accepted, in BACKLOG. **MPN-in-a-sentence guard (Decision #255):** this vetting path is gated upstream by the greenfield classifier `isGreenfield` ([llmOrchestrator.ts](lib/services/llmOrchestrator.ts)), which keyed only on `!looksLikeMpn(userText)` — a WHOLE-MESSAGE check, always false for a multi-word sentence — so a request that *named* a specific MPN inside prose ("I need replacements for BC847CLT3G from On Semi") was misrouted here, fabricating specs from the named part and tagging its keyword neighbours "Below spec." Fixed with `mentionsMpn(text)` ([searchSummary.ts](lib/services/searchSummary.ts)): token-scans for an MPN *inside* a phrase while excluding value/range/size/package/qualification tokens (12V, 1–2mA, 0805, X7R, SOT-23, AEC-Q200 — asymmetric on purpose: a false positive only loses fit-ranking, a false negative reintroduces wrong "Below spec"), AND'd into `isGreenfield`. The non-greenfield `search_parts` branch additionally drops any model-attached `partType`/`constraints`, and `searchParts` ignores `constraints` when `looksLikeMpn(query)` (defense-in-depth) — so spec-vetting can never run on a part-number turn. **Any "does the user reference a specific part?" check needs token-level scanning, not `looksLikeMpn` (whole-message).** Sibling audit-driven hardening (same Decision #255): the `present_choices` hard line ("a choice button names a requirement CATEGORY or workflow action, NEVER a specific part") is now enforced in CODE, not just the prompt — [choiceGuard.ts](lib/services/choiceGuard.ts) `sanitizeChoiceOptions` (wired into the tool handler) strips `mpn`/`manufacturer`, neuters `action:'confirm_part'`→`'other'`, and DROPS any choice whose label names a part (`mentionsMpn`, sparing categorical labels like "N-channel"/"X7R"/"AEC-Q200"); the tool schema dropped the contradictory `confirm_part`/`mpn`/`manufacturer` affordances. Only LLM-authored choices pass through; the app's own next-step choices (`find_replacements`/`show_mfr_profile`/`show_best_price`, built client-side from resolved parts) are untouched.
- **Clickable Atlas-MFR mentions in chat + #166 hardening** (Decision #203, amended Jun 1, 2026): chat MFR names render as clickable links → opens the same right-hand `ManufacturerProfilePanel` used by recommendation-card MFR clicks. Atlas-only scope. Built via combined regex in [components/MessageBubble.tsx](components/MessageBubble.tsx) (`buildLinkPattern(mpns, mfrs)` returns `{ regex, mpnGroup, mfrGroup }`, one linear pass, capture-group dispatch — single-pass so MFR + MPN matches don't double-wrap; MFR styling drops monospace). `AppShell.knownAtlasManufacturers` memo unions FIVE sources: `r.part.mfrOrigin === 'atlas'` (note: lives on `Part`, NOT `XrefRecommendation`), `m.dataSource === 'atlas'` on search matches + source part, `useManufacturerProfile.atlasNamesQueried` (state Set updated in `applyResult` when panel-click resolves to atlas), and `AppState.chatAtlasMfrs` (state Set fed by `OrchestratorResponse.mentionedAtlasManufacturers: string[]` populated server-side from each `get_manufacturer_profile` tool call with `source: 'atlas'`). The last source is the **cold-ask wire** (added Jun 1) — without it, asking the LLM about a MFR you haven't seen on a card produced an answer with no clickable name (the LLM's server-side tool lookup was invisible to the client). Same `mentionedAtlasManufacturers` field on `ListAgentResponse` for parity. Decision #166 hardening shipped alongside: `designResources` added to projection (was mapped but dropped — same shape as the GigaDevice `stockCode` bug); all optional fields `?? null`-normalized so `JSON.stringify` doesn't silently drop the key; `get_manufacturer_profile` + condensed `MFR_CLAIM_DISCIPLINE_MINI` paragraph exposed to `refinementChat()` and `listChat()` (previously only `chat()`); tool def + handler hoisted to shared `GET_MFR_PROFILE_TOOL` + `executeGetManufacturerProfile()` at module scope to prevent drift across the three orchestrators.
- **Chat manufacturer-discovery tool** (Decision #258): `find_component_manufacturers` answers "which/who/are there manufacturers that make X" in `chat()` only — distinct from `get_manufacturer_profile` (one named MFR) and `search_parts` (specific parts); without it the model fell to a part search, got zero, and emitted the misleading "no parts matching your criteria" line. Flexible across THREE grains via [atlasManufacturerDiscovery.ts](lib/services/atlasManufacturerDiscovery.ts) `resolveDiscoveryScope`: **specific family** ("BJTs"→`family_id`), **component supertype** ("capacitors"→`atlas_products.category`, the ComponentCategory enum — broad, indexed), **high-level group** ("passive components"/"discrete semiconductors"/"ICs"→registry `LogicTable.category`-derived family set). Load-bearing **bare-supertype-vs-qualified-family arbitration** (`meaningfulRemainder` — accept the specific family only if a qualifier survives stripping the supertype head word): "resistors"→all 4 resistor families, but "tantalum capacitors"→family 59; "logic ics"→Logic ICs supertype, not the ICs group. One jsonb RPC `get_atlas_manufacturers_for_scope(p_family_ids, p_categories, p_limit)` ([scripts/supabase-atlas-manufacturers-by-scope-rpc.sql](scripts/supabase-atlas-manufacturers-by-scope-rpc.sql)) does the GROUP BY (**`RETURNS jsonb` dodges the 1000-row cap** per Decision #206; the SQL is run once in Supabase — single instance, no separate prod env; applied & verified live June 27). Returns the TRUE distinct count + top-30 by volume (`truncated` flag so the cap isn't read as "only N exist"); listed names register in `mentionedAtlasManufacturers` so they linkify to profile panels (Decision #203 wire). **Chinese/Atlas-only scope**, stated in tool + prompt. Folded in: removed the stale hardcoded "~115 Chinese manufacturers" from six chat-prompt blocks (identity table is 1,000+; counts now live from the RPC). Follow-up in BACKLOG: profile pull should open the right-hand panel (P3).
- **System-driven guided part selection** (Decision #262, MERGED June 30 2026 in `2499498` — note it shipped WITHOUT running its own "blocks merge" validation checklist, which is why the two regressions in Decision #270 and the Road-A/Road-B work went unnoticed): greenfield selection in chat ("I need a voltage regulator", no MPN) asks a family's REQUIRED specs one step at a time, **fully system-driven — the model is BYPASSED on guided turns** (Phase 3). `decideGuidedTurn` ([lib/services/guidedSelectionController.ts](lib/services/guidedSelectionController.ts)) runs at the TOP of `chat()` before the agentic loop: when it owns a turn it returns the system-authored response directly (fixed-wording question + system buttons, OR it runs `searchParts` ITSELF with the tracked specs attached) and `chat()` returns early. The model never phrases a guided question, never fires the guided search, can't swap a stated value — the only model call on a guided turn is the injected temp-0 `extractAnsweredSpecs` answer-parser. Phase 3 replaced a Phase 2 where the model still phrased+fired and a live test caught it improvising a `present_choices`, silently rewriting 10 Ω→10 kΩ, and running its own constraint-less search that dropped the fit chips. `guided_select` tool REMOVED; prompt's guided block slimmed to "system owns these turns; you only clarify an unrecognized component type." **Fit labels** ("Fits your specs / Below spec") are now GUARANTEED — the system-run search always carries the constraints into the logic-vetting pass; an off-spec value surfaces as "Below spec" rather than being silently rewritten. **State marker:** `OrchestratorMessage` has no metadata channel, so the controller recognizes its own reserved phrasings (`isSystemGuidedQuestion`: "Which X do you need?" / "What … are you targeting?") to detect a continuation turn. **Recognition is registry-backed, not a hand list (Phase 4 — kills the whack-a-mole):** the deterministic recognizer (`resolvePartTypeFamily` = `LABEL_TO_FAMILY` chip-label index built from `AMBIGUOUS_HEADS` → keyword `resolveFamilyFromText`) runs first (common types → 100% consistent, zero LLM); when it misses on a fresh sourcing turn, `classifyPartTypeFamily` ([llmOrchestrator.ts](lib/services/llmOrchestrator.ts)) — a temp-0 forced-tool classifier whose **enum is derived from the live 43-family `logicTableRegistry`** (complete by construction) and **output-bounded to a familyId or 'none'** (a pure classifier, never a prose author) — recognizes the long tail. So any phrasing of a known component is OWNED; only theory/MPN/non-sourcing turns defer (correctly). `AMBIGUOUS_HEADS` covers regulator→C1/C2, transistor→B5/B6/B9, capacitor→12/58/59/64/60, diode→B1/B2/B3/B4, **relay→F1/F2, thermistor→67/68, inductor→71/72** (last three added Decision #274; labels re-pin via `LABEL_TO_FAMILY`, incl. variant families B2/B4 unreachable by `resolveFamilyFromText`). The three new heads each have a *bare* subcategory key (`Relay`→F1, …), so `matchAmbiguityHead` is **bare-aware** via `resolveFamilyMatch` (which now exposes the matched key): a QUALIFIED form ("solid state relay"→F2, "NTC thermistor"→67, "power inductor"→71) pins, only the bare word disambiguates, and the disambiguation is hoisted above `pinFamily`. **Known limit (#274, BACKLOG "Over-disambiguation on qualified non-key phrasings"):** a qualified sub-type with no exact key ("reed relay", "toroidal inductor") over-disambiguates — no clean deterministic fix (the "reed⇒F1" signal needs domain knowledge). **Gates:** greenfield only; MPN gate skipped mid-questions AND when the message names a part type (`looksLikeMpn` false-positives on "Tantalum"/"MLCC"/"Fixed"); fresh ENTRY needs selection intent (not theory) + a clean screen (`hasOnScreenContext` false) so post-results refine/filter/pivot stay with the LLM; classifier gated `!isLikelyTheory && (intent || bareNoun)`; continuation is unconditional. Sanity-catches dropped for v1 (off value → "Below spec"; deterministic per-family range nudge is a BACKLOG add). **Source of truth (rebuilt by Decision #270 — read that bullet before touching the tiers):** [docs/min_attr_sets.md](docs/min_attr_sets.md) is now the *input*; `SELECTION_TIERS` is **generated** into `lib/services/selectionTiers.generated.ts` and re-exported by `selectionQuestions.ts`. Never hand-edit either. **Root bug fixed (Phase 2):** button-vs-typed is `input: 'choice' | 'value'` decided by whether a real closed option set exists (`upgradeHierarchy` or a tightened attributeName-parenthetical parse rejecting garbage like "X/Y Class"/"N/P") — NOT by `logicType` (numeric specs like `output_voltage` are `identity` exact-match yet typed). **Step controller** [guidedSelection.ts](lib/services/guidedSelection.ts) `nextGuidedStep`: choice specs ONE per turn (one button group per message), value specs batched into ONE prose turn, then search when Tier 2 complete; "any/not sure" (value null) is answered-but-dropped-from-constraints. **Admin:** read-only "Required to search"/"Narrows results" chip markers in the "Attribute Templates" section ([ParamMappingsPanel.tsx](components/admin/ParamMappingsPanel.tsx)), same `SELECTION_TIERS` data. **Known v1 limits (BACKLOG):** a 2nd guided selection later in the same chat (results on screen) falls to the LLM; an MPN typed mid-questions is re-asked. Chat-prompt change ⇒ re-run [docs/chat-prompt-regression-checklist.md](docs/chat-prompt-regression-checklist.md) (greenfield) before merge.
- **Selection questions are GENERATED from a reviewable document; the build refuses an unclassified rule** (Decision #270): [docs/min_attr_sets.md](docs/min_attr_sets.md) is the **source of truth** for which specs the agent asks a user who is choosing a part by description. `SELECTION_TIERS` is **generated** from it into `lib/services/selectionTiers.generated.ts` — **never hand-edit either the generated module or the tiers**; edit the document and run `npm run selection:audit`. **Why:** the doc was hand-copied into TS once and never revisited, so the two drifted invisibly — **536 of 823 scored specs (65%, all 43 families) had no decision recorded either way**, and the *document itself* omitted `vin_max` for C1 LDOs while requiring it for C2 switching regulators, so the app **never asked what voltage goes into a voltage regulator**. The old guard test asserted every listed id *exists*; it never asserted anything was *missing*. **THREE states** (`Required for Search` = always asked, blocks the search · `Narrows Results` = asked only when the result set is too large · `Not Asked`) — the two ask-states have *different triggers*; collapsing them means asking everything up front (27 questions before showing a MOSFET). **`npm run selection:check` runs as `prebuild`** and fails the build when a scored rule has no state, an attributeId was invented, or the generated module is stale (same check as a jest test, so `npm test` catches it too). **NO weight threshold anywhere** — a threshold is an invented heuristic that fails both ways (false-positive on `tst` w8, correctly never asked; false-negative on C1 `vin_min` w7, a plausible real hole). The build only guarantees a decision **was made**; a human reviews the decisions in the doc, with weight as *information*, never a verdict. The one automatic signal is threshold-free: **cross-family contradiction** (same attributeId asked in one family, silently skipped in a sibling that also scores it — 38 today; catches the `vin_max` bug on its own). **ONE writable surface** — the admin "Attribute Templates" panel is READ-ONLY (a file *and* a UI that both write the same truth is the exact drift being fixed) but its chip is now **never blank**, so a real omission and a deliberate one no longer look identical (`⚠️ Not asked` = nobody ruled on it). `Narrows Results` is consumed at runtime as of Decision #272 (next bullet).
- **Hear every spec the user states; ask ONE question when the pool is useless** (Decision #272): three fixes that together let *"small-signal NPN, 9V, 1-2mA, hFE 200-400"* surface the BC847B. (1) **Hear everything** — the guided answer-extractor's `attributeId` enum is now the family's **entire rule list**, not its REQUIRED specs, so a volunteered spec is no longer deleted before the search runs (it was discarding **629 of 823 scored specs** across all 43 families). *The ask-list and the hear-list are different things.* Measured safe before shipping: 0 hallucinations even at B5's 6→27 enum. (2) **Stated bands** ([statedBands.ts](lib/services/statedBands.ts)) — a rule the engine structurally **cannot compare** (`application_review`/`operational`, gated by `ruleCanCompare` in [matchingEngine.ts](lib/services/matchingEngine.ts)) returns a flat 50% for every candidate, so a stated `hFE 200-400` left gain-110/200/420 parts indistinguishable. **Do NOT retype the rule** — `application_review` is the RIGHT verdict for a *cross-reference* ("the gain differs; a human should look"); a *search* is a different question ("I asked for 200-400 and this is 420"). So the **search path** compares stated bands itself; the engine is untouched and cross-refs are unchanged. ⚠️ **Only EXPLICIT bands** (a range, or a min/max-*labelled* bound) — a bare value's direction is unknowable and **guessing one is what caused Decision #271**. ⚠️⚠️ **`vceo_max`/`vds_max`/`ic_max` are SPEC NAMES, not bound labels** — reading the `_max` in a spec's own name as a user-stated *upper* bound bands the catalog to parts rated *below* what the user asked for; resolve the attribute **as given** before stripping any min/max. (3) **The narrowing step** — `tier3` had **zero runtime consumers**; now a too-big pool triggers ONE question before presenting. **The data gets a veto, the document gets the vote**: split-quality (normalized entropy over pool buckets) + a coverage gate decide which specs *can* separate the candidates; among those, `min_attr_sets.md` order decides which to *ask*. ⚠️ **Entropy is a GATE, never a ranking** — measured, the gain-vs-Vce(sat) order **flips** depending on whether attributes come from full enrichment or the search projection, so ranking on it makes the question depend on invisible plumbing. Numeric buckets **become the buttons**, so the answer is an explicit range by construction. **Convergence:** hard cap of ONE question; the answer (**including "any"**) lands in `answeredSpecIds`, which is therefore the question counter *and* must be in the search **cache key** — without it, waiving the question reproduces the same key, the cache returns the question again, and the escape hatch becomes an infinite loop. `SEARCH_CACHE_SCHEMA_VERSION` v10→v12. Deleted: `pickFetchBand` (dead code that still contained #271's ×10 ceiling, pinned by a passing test suite).
- **Instant source-panel load — optimistic preview** (Decision #257): clicking a part (chat MPN or card) to load it as the source paints the panel INSTANTLY from data already in memory instead of a blank skeleton, while the canonical attributes fetch in the background. `buildOptimisticFromRec`/`buildOptimisticFromSummary` ([optimisticAttributes.ts](lib/services/optimisticAttributes.ts)) synthesize a partial `PartAttributes` (rec.part Overview + matchDetails Specs, or a PartSummary header + keyParameters); `handleConfirmPart(part, optimistic?)` threads it into both confirm flows' initial setState (`sourceAttributes: preview ?? null`); layouts gate the skeleton `loading={phase==='loading-attributes' && !sourceAttributes}`. The preview is display-only — `sourceAttrsReadyRef` (false during preview, true once canonical attrs commit) gates the `handleSearch` intent shortcut so replacement-finding never scores a partial preview. Source-agnostic (fast for ANY part). On top of this, `handleMpnClick`'s rec PartSummary carries `dataSource: mfrOrigin==='atlas'?'atlas':undefined` so the BACKGROUND fetch routes straight to Atlas (skips the slow Digikey-first gauntlet for Chinese parts). Mirrors `handleSelectRecommendation`'s optimistic-open for the comparison view.
- **Recommendation pipeline performance** (Decision #98, #138): UI passes pre-fetched `sourceAttributes` to `/api/xref` POST to skip duplicate `getAttributes()` call. FindChips candidate enrichment is deferred — recs display immediately, then `triggerFCEnrichment()` sorts recs by display priority (via shared `sortRecommendationsForDisplay()` helper), fires a smaller 30-MPN priority chunk first + subsequent 50-MPN chunks in parallel via `Promise.all`, and merges each chunk into state as it returns so the top-of-list cards populate within seconds (FindChips rate caps were raised — it's an internal API — so a high-candidate part fans out in seconds rather than the old 60/min serialization; Decision #254). `isEnrichingFC` flag drives a `Loading pricing…` placeholder on cards during enrichment. QC logging is fire-and-forget. See `triggerFCEnrichment()` and `showRecsAndDeferAssessment()` in `useAppState.ts`. **(Decision #251 briefly gated this behind a remembered toggle; Decision #252 reverted that — FC enrichment auto-fires on every recs load again. See next bullet.)**
- **Replacements price/stock always loads; toggle is display-only** (Decision #251, **reverted by Decision #252**): #251 made the single-part Replacements panel's FindChips fetch opt-in behind the "Price & stock" toggle. That left the **Commercial tab inside each replacement card empty** (it reads `part.supplierQuotes`, which only `triggerFCEnrichment()` populates) with no way to load it — a dead end. **Decision #252 reverts the gating**: FindChips is our internal API with no rate limits, so `triggerFCEnrichment()` now fires **unconditionally** on every recs load (`showRecsAndDeferAssessment`, the parts.io re-score tail, and `hydrateState`) — the Commercial tab is never empty and there's no per-click wait. The "Price & stock" toggle (`AppState.commercialEnabled` + `handleToggleCommercial()` in `useAppState.ts`) is now **display-only**: it shows/hides the price chips on the suggestion cards and never gates the fetch (`commercialFetchedRef` removed entirely). It **defaults ON** and stays a **remembered preference** in `localStorage` (`xrefs:show-commercial` — only an explicit `'0'` hides; `readCommercialPref()` returns `true` by default; `initialState.commercialEnabled` is `true`, safe because the panel only renders once recs exist). `RecommendationsPanel` renders it as a compact labeled `Button` (outlined off / contained on / "Loading…" while `isEnrichingFC`), controlled-with-fallback (`commercialEnabled`/`onToggleCommercial` props threaded `AppShell → DesktopLayout/MobileAppLayout`); the parts-list modal omits the props and falls back to local display-only state (its recs are already FC-enriched). The BOM parts-list FC flows are untouched. Display/fetch-timing only — no cache bump.
- **Post-recs chat summary is deterministic** (Decision #173): `buildRecsSummary()` in `lib/services/recommendationSummary.ts` posts a 1-2 line summary built directly from rec fields. The previous LLM-driven engineering assessment was removed after three rounds of prompt tightening failed to stop Sonnet from fabricating MFR origin / cert / supply-chain prose. User-driven follow-up Q&A still routes through `chatWithOrchestrator` in `handleSearchWithLLM` with the existing system-prompt discipline rules. Bundled-filter path (e.g., "Chinese replacements") skips the deterministic summary because `dispatchFilterIntent` already posts an equivalent message.
- **Suggestions panel shows ALL candidates; Active-first within bucket** (Decision #232, supersedes #226): the single-part Replacements panel hides **nothing** by default — every candidate renders regardless of lifecycle status (Active/Obsolete/Transferred/…) or match quality. Active is surfaced by **ranking, not hiding**: `sortRecommendationsForDisplay` ranks by `statusRank` (Active=0 else 1) — **promoted to the TOP-LEVEL sort key by Decision #244** (here it was a within-bucket tiebreak: after the Decision #236 least-fails primary + Accuris→MFR→Logic bucket), so Active now floats above **every** non-Active part regardless of bucket/score, not just to the top of each bucket. The only panel filters are explicit/user-initiated (manufacturer / CN-only / category / zero-stock / AEC-qualified — Decision #238 — and a **lifecycle-status checklist** added June 17, 2026: per-status checkboxes Active/Obsolete/Discontinued/NRND/Last Time Buy, **all checked by default** so nothing is hidden and the per-status + external chip/chat counts stay reconciled; `hiddenStatuses: Set<PartStatus>` empty=all-shown, unchecking adds to the set, `.filter(r => !hiddenStatuses.has(r.part.status))` on the candidate chain, `activeHiddenStatuses` drives a "Hiding: …" chip; display/sort only, no cache bump). This **supersedes Decision #226**: that work reconciled chat/Overview counts with a panel that hid a subset — with nothing hidden, "displayed count" == full count, so the `isDefaultDisplayed`/`getDefaultDisplayedRecs`/`DEFAULT_MAX_MISMATCHES` predicate was removed and `buildRecsSummary`/`summarizeRecommendations`/`dispatchFilterIntent`/`summarizeCrossRefs` (in `AttributesTabContent`) all count the full set. parts.io candidate statuses are normalized via `mapPartsioStatus()` (exported from `partsioClient.ts`) at both parts.io→Part sites in `partDataService.ts` — raw codes like "Transferred"/"Acquired"/empty → `'Active'` (still orderable, shown green, sort to top), only true end-of-life (Obsolete/Discontinued/EOL→NRND/LTB) sink. Without this, parts.io crosses weren't recognized as Active and ranked alongside obsolete. Scope: suggestions panel only — the BOM parts-list still prefers Active for its auto-picked replacement. The status-value change bumped `RECS_CACHE_SCHEMA_VERSION` v16→v17 and `BASE_RECS_SCHEMA_VERSION` v5→v6.
- **Source panel = part inventory; filters are view-scoped; clickable cross-ref chips** (Decision #229): the single-part view's two cross-reference surfaces are now wired with one principle — **the source panel ("Cross References" section at the bottom of Overview) shows the part's FULL replacement inventory; filters belong to the right (Replacements) panel only.** Three parts: (1) **Clickable chips** — the source-panel category chips (MFR/Accuris/Logic) + "Manufacturers with crosses" chips are clickable filters that drive the right panel via shared state lifted into `DesktopLayout` (`xrefCategory`/`xrefMfr`, mirrors the `activeAttributesTab` lift); `RecommendationsPanel`'s `selectedCategory`/`selectedMfr` became controlled-with-fallback props (modal-chat usage stays local). Active chip renders filled to mirror the popover selection. (2) **Source panel reads the UNFILTERED base** — `DesktopLayout` feeds `<AttributesPanel allRecommendations={appState.allRecommendations}>` (the preserved full set) NOT the chat-filtered `recommendations`, so "show me Chinese replacements" no longer narrows the source panel's MFR row. The chat filter was already non-destructive (`dispatchFilterIntent` keeps the full set in `state.allRecommendations`, narrows only `state.recommendations`; enrichment re-applies `currentFilter` against the base). `summarizeCrossRefs` counts over `getDefaultDisplayedRecs(base)` so the row lists every MFR with a *solid* (non-obsolete, ≤2-fail) cross — all origins, default quality (extends Decision #226's predicate to this 4th count surface). (3) **Source-chip clicks clear any active chat filter** via new `clearChatFilterSilently()` in `useAppState.ts` (restores `recommendations=allRecommendations`, nulls `currentFilter`/`currentFilterLabel`, NO chat messages — unlike `dispatchClearFilter`; no-op `return prev` when none), threaded AppShell → DesktopLayout → the chip handlers, so chips always filter from the full inventory rather than the narrowed subset. Also reconciled the panel header: counts (`shownCount`/`hiddenInScope`/scoped `qualityHiddenCount`/`statusHiddenCount`) compute over a `scoped` set (recs after explicit MFR/category/CN/zero-stock filters) so "N shown · M hidden" tracks the active filter and reconciles with the cards; the ">N fails" dismissible chip became a count-less control (`Hiding >N fails`) so two "hidden" numbers no longer compete. Display/interaction only — **no `RECS_CACHE_SCHEMA_VERSION` bump**. **Superseded in part by Decision #232:** the panel no longer hides by status/quality, so the `scoped`/`shownCount`/`hiddenInScope`/`qualityHiddenCount`/`statusHiddenCount` header machinery and the "Hiding >N fails" chip were removed, and `summarizeCrossRefs` now counts the full set (chip count still = what a click surfaces, because a click surfaces everything). The clickable-chip wiring and `clearChatFilterSilently` are unchanged.
- **Per-attribute acceptance criteria** (Decision #238, builds on #236/#237): users loosen matching on individual source attributes from the Specs panel. Unified `AcceptanceCriterion = {kind:'range',percent} | {kind:'set',values}` (`lib/types.ts`), threaded through `getRecommendations` → POST `/api/xref/[mpn]` → `useAppState` (`acceptanceCriteria` state + `acceptanceCriteriaRef`, session-only; cleared on reset/new-part). In the full-result cache variant (key `accept`); `RECS_CACHE_SCHEMA_VERSION` v17. Since Step 2 (below) the base variant ALSO keys on the *fetch-affecting* subset of criteria (`fetchWideningKey`), `BASE_RECS_SCHEMA_VERSION` v6 — rescore-only criteria stay out of the base key. `applyAcceptanceCriteriaToLogicTable` (`acceptanceModifier.ts`, mirrors `contextModifier`): `range`→raise-only `tolerancePercent` on identity rules; `set`→`MatchingRule.acceptedValues`, consumed by a rule-type-agnostic short-circuit in `evaluateRule` that passes when a candidate's `normalize()`d value is in the set. `set` is restricted to identity-family rules in the modifier so it can't bypass a numeric SAFETY gate (threshold/fit/vref). UI: hover tune-icon → inline RangeEditor (slider) or SetEditor (checklist, options from candidate `matchDetails`); changing a criterion auto-re-scores the panel via the deferred parts.io path. **Step 2 — fetch-widening SHIPPED** (`lib/services/fetchWidening.ts`, single source of truth for the fetch path AND the base cache key): keyword-driving attrs now widen the candidate FETCH. **Digikey = E-series keyword fan-out** (`fetchDigikeyCandidates` fans out one `keywordSearch` per in-band E-series value (`range`) / accepted value (`package_case` `set`), union+dedup, capped at `MAX_WIDEN_QUERIES`; `buildCandidateSearchQuery` gained value-token substitution). **Step 3 — Digikey parametric value-grid widening SHIPPED (June 16, 2026)** for non-E-series numeric attrs (voltages/frequencies/etc.) via the full parametric ValueId filter: two-call discover→apply in `fetchDigikeyCandidates` — `getCategoryParametricFacets('', categoryId)` (empty keyword so the facet covers the whole category) → `findFacetForAttribute` (reuses the existing forward param map; facets use `ParameterName` and carry their own `Category`) → in-band values by `ProductCount` DESC (cap `MAX_PARAMETRIC_VALUES`=25 so standard E-series neighbors survive) → `parametricFilterSearch` (`FilterOptionsRequest.ParameterFilterRequest`) → in-band re-verify (drops mis-keyed/unfiltered results); single-attr (first eligible, mirrors `computeAtlasWidening`), latency bounded to ~2 round-trips via two-phase `Promise.all`. **Atlas = `fetch_atlas_candidates_widened` RPC** (`scripts/supabase-atlas-candidates-widened-rpc.sql`) applies the numeric band BEFORE `.limit(50)` (filter-before-limit fixes value-band starvation; keeps missing-numericValue rows as review candidates + CASE-guards the cast; `fetchAtlasCandidates` falls back to the default fetch on an empty band so widening never returns fewer than un-widened). **Eligibility:** `RANGE_FETCH_ATTRS` mirrors the full UI `RANGE_ELIGIBLE_ATTRIBUTE_IDS` — every numeric band widens at least the **Atlas** fetch (generic numeric RPC, no enumeration). The **Digikey** keyword fan-out covers `ESERIES_ENUMERABLE_ATTRS` (resistance/cap/**inductance** — E-series-stocked); voltages/frequencies/impedance/etc. (`RANGE_FETCH_ATTRS` minus `ESERIES_ENUMERABLE_ATTRS`, predicate `isParametricWideningCriterion`) now widen on Digikey via the Step-3 parametric filter AND on Atlas. Attrs with no Digikey param-map entry (e.g. `izt`) can't match a facet → stay Atlas-only (acceptable). Deferred: multi-attr parametric widening; parts.io value-search — see BACKLOG. Only Digikey + Atlas widen at all (value-search sources); parts.io/Mouser/MFR-crossref are MPN-keyed equivalence lookups with nothing to widen (they still get Step-1 scoring relaxation). **AEC removed from the acceptance control (June 16, 2026):** AEC was the original `set` demonstrator but is a binary requirement, not a tolerance — non-qualified parts carry *missing* AEC data (not "No"), which the checklist can't offer and the engine short-circuit can't rescue, so it was inert. `SET_ELIGIBLE_ATTRIBUTE_IDS` is now empty (the `set` mechanism awaits a genuine categorical like dielectric); the "automotive matters" intent moved to the `aec_qualified_only` Replacements filter (see Filter pipeline bullet). Acceptance is reconciled with `filterAutomotiveAecMismatches` (via the flipped rule result) but deliberately does NOT override the qualification-domain safety gate. Full status + deferred review items: `docs/acceptance-criteria-followups.md`.
- **Comparison Specs rows are aligned across both panels** (Decision #246): in the single-part "comparing" view the source (left, `AttributesPanel`) and replacement (right, `ComparisonView`) Specs tables render from ONE shared, unioned, ordered row set so every attribute occupies the same row index on both sides, with a muted "—" where a side has no entry. `buildComparisonRows(sourceAttributes, replacementAttributes, recommendation)` in [lib/services/comparisonRows.ts](lib/services/comparisonRows.ts) (returns `AlignedSpecRow[]`) joins recognized source params (by `sortOrder`) with replacement-only matchDetails (appended last, `ruleResult && !== 'pass'`), **no longer dropping source-only rows** (the old `ComparisonView` filter that caused the drift). `DesktopLayout` computes it once when `phase==='comparing'` and passes the **same array reference** to both panels (`comparisonRows` prop on `AttributesPanel`, `rows` prop on `ComparisonView`). In `AttributesPanel` a `paramById` lookup distinguishes real source rows (keep acceptance-tune controls + D/P/A badge) from replacement-only rows (inert slot-holder); prop absent ⇒ standalone rendering unchanged; the unrecognized-Atlas "extras" toggle stays a source-only appendix below the aligned region. `ComparisonView.rows` is optional and self-builds when not supplied (mobile / parts-list modal). Datasheet-only rules (e.g. SOA) show the engine's `N/A` on **both** sides (honest, aligned) — the "—" is only for genuine `null`. Display-only, **no cache bump**. Accepted caveat: a left-panel acceptance-editor `Collapse` transiently shifts rows below it until closed (not mirrored on the right).
- **Filter pipeline** (Decisions #131 filter intents, #171 origin filter, #268 per-status lifecycle): `applyRecommendationFilter(recs, FilterInput)` in `lib/services/recommendationFilter.ts` is the shared predicate used by both the LLM `filter_recommendations` tool and the client-side `detectFilterIntent` intercept (`lib/services/filterIntentDetector.ts`). Supported predicates: `manufacturer_filter`, `min_match_percentage`, `exclude_statuses`, `exclude_failing_parameters`, `attribute_filters`, `category_filter`, `mfr_origin_filter` (`'atlas' | 'western'`), `aec_qualified_only` (keep only AEC-Q100/Q101/Q200 parts via `isAecQualified()` — matchDetail `Yes` OR `part.qualifications` badge; inclusive-keep so it sidesteps missing-AEC data; surfaced as the "AEC-qualified only" toggle in the Replacements filter popover, the binary "automotive matters" control for families not in `AUTOMOTIVE_AEC_ENFORCEMENT`). Origin filter keys off `XrefRecommendation.part.mfrOrigin` (set per Decision #161); detector recognizes Chinese / Asian / China / PRC / mainland / made-in-China and Western / American / European / non-Chinese phrasings without requiring a filter verb.
- **Lifecycle status is PER-STATUS, never a single "obsolete" flag** (Decision #268): `PartStatus` has FIVE values (`Active | Obsolete | Discontinued | NRND | LastTimeBuy`) and they are **distinct** — "hide discontinued" must not touch Obsolete. Both filters take `exclude_statuses?: PartStatus[]` and share ONE implementation (`resolveExcludedStatuses` / `statusIsExcluded` / `describeExcludedStatuses`, exported from `recommendationFilter.ts`, imported by `searchResultFilter.ts`) so the two predicates can't drift. **Missing status ⇒ treated as `Active`** — missing data never hides a part. Legacy `exclude_obsolete` is an alias for `['Obsolete']` that UNIONS with (never overrides) the precise list. `detectStatusIntent` maps each word to exactly the status it names; only the genuine group words (EOL / end-of-life / inactive / dead) and "only active" expand to every non-Active status. **The original bug is the cautionary tale:** one boolean implemented as `status !== 'Obsolete'`, fed by a detector that collapsed every lifecycle word into it — so "hide discontinued" deleted the user's OBSOLETE parts and left the discontinued ones, and the test suite stayed green because no fixture carried a `Discontinued` part.
- **Search-card filters are intercepted DETERMINISTICALLY** (Decisions #263 origin, #268 status): when search-result cards are on screen, `handleSearch` (`useAppState.ts`) intercepts clear → origin (`detectSearchOriginRefinement`) → status (`detectSearchStatusRefinement`) client-side and routes them through one `dispatchSearchFilter(input, label, query)`, never the LLM. Rationale: the model reaching for `filter_search_results` is a coin-flip, and **a filter that silently never fires is indistinguishable from one that fired and matched nothing** — both read as "the app ignored me". Filters narrow from the preserved full set (`searchFullMatchesRef`) so they compose; every applied filter reports the arithmetic ("Filtered to 3 of 11 — hiding Discontinued (8 hidden)"); a zero-match filter says so and LEAVES THE CARDS UP rather than emptying the panel. `currentSearchFilterLabel` + `dispatchClearSearchFilter` make an active card filter visible and clearable. Both detectors are guarded by `namesComponentType` so "only active MLCCs" over transistor cards stays a PIVOT (new search), not a refine. **Attribute filtering on search cards is NOT built** (cards carry ~3 `keyParameters` + a description, no `matchDetails`) — see BACKLOG.
- **Search-result origin filter is deterministic, mirrors the flag** (Decision #263): "show me only the Chinese ones" over search-result CARDS (not recs) is applied client-side by `detectSearchOriginRefinement` ([filterIntentDetector.ts](lib/services/filterIntentDetector.ts)) + `dispatchSearchOriginFilter` ([useAppState.ts](hooks/useAppState.ts)) — NOT via the LLM, which refused to call the `filter_search_results` `mfr_origin_filter` tool (its "never assert MFR origin" discipline, Decision #166, prose-answered "none are Chinese" instead). `searchParts` resolves `PartSummary.mfrOrigin` per unique MFR (alias resolver, mirrors `getRecommendations`). **The filter predicate MIRRORS the card's 🇨🇳 flag** ([searchResultFilter.ts](lib/services/searchResultFilter.ts)): Chinese = `mfrOrigin === 'atlas' || dataSource === 'atlas'` (the flag renders off `dataSource === 'atlas'` in [PartOptionsSelector.tsx](components/PartOptionsSelector.tsx), and a cached card can carry `dataSource` without a resolved `mfrOrigin`); Western = `mfrOrigin === 'western' && dataSource !== 'atlas'`. `SEARCH_CACHE_SCHEMA_VERSION` v8→v9. **Refine-vs-new-search** keys off a real component vocabulary — `namesComponentType` in client-safe [componentVocabulary.ts](lib/services/componentVocabulary.ts) (`GROUP_SYNONYMS`+`SUPERTYPE_SYNONYMS` shared with [atlasManufacturerDiscovery.ts](lib/services/atlasManufacturerDiscovery.ts), can't drift): only a NAMED component ("Chinese MLCCs") is a new search, descriptor phrasings ("PRC-based", "Chinese firms only") refine; failure mode is safe (refine, not leak-to-LLM). The intercept guard keys off the CURRENT view (`searchResultRef`), never a stale full-set ref. **Flag consistency** (Decision #263): `getRecommendations` now also forces `mfrOrigin='atlas'` on Atlas-sourced candidates (mirrors `searchParts`) so an Atlas maker the alias index misses reads 🇨🇳 the same in search + recs; `RECS_CACHE_SCHEMA_VERSION` v18→v19. The two filter predicates (search vs recs) are intentionally left independent — each fits its own data completeness; the resolver fix makes them agree on the same maker.
- **Bundled-search-intent routing** (Decision #172): when a search query bundles a follow-up intent ("find Chinese replacements for X"), `handleSearch` stashes both `pendingIntent` (state) and `pendingIntentQueryRef` (ref). After part confirmation, `tryAutoFireIntent` consumes both — for `find_replacements`, the stashed query is moved into `pendingPostRecsFilterRef` so the existing `showRecsAndDeferAssessment` consumer applies any bundled qualifier ("Chinese", "≥80%") once recs land. Stale-closure trap: `handleFindReplacements` accepts a `partOverride` parameter (parallel to the `contextOverride` from Decision #155) so callers on the auto-fire path bypass the captured-state read of `state.sourcePart` / `state.sourceAttributes`. `dispatchIntent` also sync-primes `sourceAttributesRef.current` before invoking `handleFindReplacements` (the useEffect mirror lags by one render).
- **Per-list views** (Decision #81): Views stored per-list in Supabase `parts_lists.view_configs` JSONB. Global views are templates (`useViewTemplates` in localStorage) used to seed new lists. `useListViewConfig` hook manages per-list views with 500ms debounced Supabase persistence. "Save as Template" strips `ss:*` columns via `sanitizeTemplateColumns()`. Lists with null `view_configs` auto-migrate from templates on first load. Default (starred) view: `defaultViewId` in ViewState JSONB, applied by `useListViewConfig` on initialization (overrides `activeViewId`). Star icon in `ViewControls.tsx` calls `setDefaultView(activeView.id)`.
- **Column portability**: Templates may only contain portable column IDs (`sys:*`, `mapped:*`, `dk:*`, `dkp:*`). `ss:*` (raw spreadsheet index) columns are list-specific and stripped by `sanitizeTemplateColumns()` before saving to template library.
- **mapped:cpn**: Optional Customer Part Number / Internal Part Number column. Auto-detected from headers in `excelParser.ts`. `cpnColumn` on `ColumnMapping`, `rawCpn` on `PartsListRow`. Resolved at render time like `mapped:manufacturer`.
- **mapped:unitCost** (Decision #156): Optional current-cost column. Auto-detected via `UNIT_COST_PATTERNS` in `excelParser.ts` (longer phrases first, "cost"/"price" last). `unitCostColumn` on `ColumnMapping`, `rawUnitCost` on `PartsListRow`. Powers `sys:priceDelta`. Numeric parsing via shared `toNumber()` from `lib/calculatedFields.ts`.
- **sys:priceDelta** (Decision #156): Built-in "Repl. Savings" column (group "Replacements"). Computes `toNumber(rawUnitCost) − resolveReplacementUnitPrice(row)`. Positive=savings, negative=more expensive, undefined=missing. Mirrors `sys:top_suggestion_price` for picking the best cross-distributor quote (FindChips supplierQuotes → part.unitPrice fallback). Signed currency display, green/red color, sortable.
- **Calculated-column header tooltips** (Decision #259): every computed/derived parts-list column carries an optional `description` on its `ColumnDefinition` (`lib/columnDefinitions.ts`), rendered as an `InfoOutlinedIcon` + `Tooltip` in the table header (`PartsListTable.tsx`, reusing the "Matched from your data" 12px adornment pattern — both header branches share one DRY `adornments` fragment) and as muted secondary text in `ColumnPickerDialog`. Set on the `sys:*` Replacements columns, `commercial:*`, `fc:*`, and `mapped:unitCost`; the two savings tooltips explicitly say a unit-cost column is required (self-documents the empty-savings case). User `calc:*` columns get "Your formula: <left> <op> <right>" via the pure `describeFormula(formula, resolveLabel)` in `lib/calculatedFields.ts` (label resolver injected to avoid a `columnDefinitions` import cycle), wired in `PartsListShell.calcColumnDefs`. Raw columns (`ss:*`, `mapped:mpn`) carry no description → no icon. Display-only, no cache bump.
- **Column display label convention** (Decision #157): `getColumnDisplayLabel(col)` in `lib/columnDefinitions.ts` appends a source suffix based on `col.dataSource`. Map: `digikey→DK`, `partsio→P`, `atlas→A`, `findchips→FC`, `mouser→Mouser`. Idempotent. Routed through table header (`PartsListTable.tsx`), column picker (`ColumnPickerDialog.tsx`), and calculated-field operand picker (`CalculatedFieldEditor.tsx`). Picker's `SourceBadge` chip is suppressed for any source covered by the suffix map. To add a new source: add one line to `SOURCE_SUFFIX`. "Manufacturer" abbreviated to "MFR" on `mapped:manufacturer` and `dk:manufacturer`.
- **Picker visibility for `ss:*` columns** (Decision #156): `useColumnCatalog` keeps any `ss:*` whose `effectiveHeaders[index]` is a non-empty trimmed string. Previously a strict `nonEmptyIndices` data scan silently dropped numeric/currency columns. Unlabeled placeholder columns still require actual data.
- **Master-view save reverse-mapping** (Decision #156): On master-view save in `PartsListShell.tsx`, `reverseMapKnownColumns()` runs before `sanitizeTemplateColumns()`. Picker expands `mapped:*` → `ss:N` on open; without reverse-map, sanitize would strip every Your Data column. Applies to both create and edit master-scope flows.
- **Manual part addition** (Decisions #113, #115): Empty lists can be created via third "Empty List" tab in `InputMethodDialog`. Individual parts added via `AddPartDialog` — two-step Search → Select flow: user enters MPN + optional MFR, clicks Search, picks from a results list (multi-source: Digikey/Atlas/Parts.io), then the verified `PartSummary` is passed to `handleAddPart()`. New part inserted at top of list with highlight animation. Two-phase validation: row appears immediately with resolved identity (status: 'validating'), full attributes + recommendations merge in background. Quick search via `/api/parts-list/search-quick` (search only, ~500ms, skipMouser). `skipSearch` flag on validate route skips redundant search when MPN already confirmed. Inline cell editing: `ss:*` columns are double-click-to-edit; MPN/MFR edits trigger debounced re-validation. Mouser API timeout (8s per attempt) and Digikey API timeout (10s per attempt) prevent batch validation from hanging. Batch validation passes `skipMouser: true` to `getAttributes()` and `getRecommendations()` since Mouser provides only commercial data. Progressive Mouser enrichment runs every 10 resolved rows during validation (not only at end). Cancel validation via Stop button (AbortController on all streams). Notification snackbar alerts on data source failures with Retry button for Mouser. Validation concurrency: 5 parallel items.
- **Memoized `TriageRow` — pass primitives, never containers** (Decision #269): the Triage row is **162 MUI elements** (35 Popper-backed `Tooltip`s). It was inlined in the parent's `.map()`, so any parent state change re-rendered every row on screen (~57,000 elements at 350 rows) — measured main-thread blocks up to **14s from scrolling alone**. It is now extracted + `React.memo`'d in [GlobalUnmappedParamsTable.tsx](components/admin/atlasIngest/GlobalUnmappedParamsTable.tsx). **Three invariants keep it working, and breaking any of them silently makes the memo a no-op (it will LOOK fine and change nothing):** (1) every row input is a **prop**, and whole-collection reads are resolved to per-row **primitives** in the parent (`selected.has(p)` → `isSelected: boolean`, never the `Set`); (2) every row callback is **identity-stable** — handlers that need `states`/`notesByParam` read them via **live ref mirrors** (`statesRef` etc.) so their deps stay `[]`. **Refs are safe for EVENT HANDLERS only**; a render-time ref read inside a memoized row would go stale, so render helpers are inlined into the row and props-driven; (3) any function whose result becomes a prop must return a **stable identity** (`getFamilyDisplayName` is cached by familyId — it used to return a fresh `{short, full}` object per call, which alone would have defeated the memo for every auto-flagged row). **Also:** bulk Triage should be run against a **production build**, not `npm run dev` — the dev React build instruments every fiber, making a memo bail-out cost ~4ms/row instead of microseconds (~4.5× end-to-end, measured).
- **Supabase 1000-row limit** (Decisions #123, #206, #232, #249): Supabase/PostgREST caps results at 1000 rows server-side — and this bites **both** a plain `.from(table).select()` **and** a `RETURNS TABLE`/`RETURNS SETOF` RPC. `.range(0, 99999)` does NOT defeat it (the cap is server-side). Two safe patterns: (a) paginate with a `fetchAllPages()`/`.range(from, from+999)` loop that **STOPS on error** (a failed page must not silently truncate the loop — Decision #183); (b) push aggregation into an RPC that **`RETURNS jsonb`** (wrap the final SELECT in `jsonb_agg(row_to_json(t))`) — a scalar return is never row-limited, whereas `RETURNS TABLE` re-introduces the cap. "Use an RPC" alone is NOT sufficient — the return type is the load-bearing detail. jsonb examples: `get_manufacturer_product_stats`, `get_atlas_growth_aggregates`, `get_atlas_coverage_aggregates`. Diagnostic when an on-screen count "feels stuck": compare `SELECT COUNT(*)` ground truth to the query/RPC result — a suspiciously round number (1000) is the cap signature.
- **Manufacturer name mismatch**: `atlas_products.manufacturer` uses English-only names (e.g., "ISC") while `atlas_manufacturers.name_display` has combined "ENGLISH Chinese" format (e.g., "ISC 无锡固电"). Admin product aggregation folds across every known variant via `[name_display, name_en, name_zh, ...aliases]` (Decision #148).
- **MFR profile panel + identity-based CN flag** (Decision #161): Side panel that opens on MFR-name click is now wired to `atlas_manufacturers` instead of the legacy ~10-MFR mock map (which silently no-op'd for every Chinese MFR). `lib/services/manufacturerProfileService.ts` resolves through `resolveManufacturerAlias()` → fetches the matching atlas row → maps to `ManufacturerProfile`. Falls back to `mockManufacturerData.ts` for Western MFRs not yet in `manufacturer_companies`, then to empty-state for unknowns. Endpoint `GET /api/manufacturer-profile?name=<encoded>` (auth-guarded). Hook `useManufacturerProfile` is async with placeholder-on-open + in-flight request id. Country flag is now keyed off `XrefRecommendation.mfrOrigin` (resolved per unique MFR in `getRecommendations()` via Promise.all over the alias resolver, 5-min cached) rather than `dataSource === 'atlas'` — so 3PEAK shows 🇨🇳 on every card regardless of whether attributes came from Digikey, Atlas, Mouser, or parts.io. RECS_CACHE_SCHEMA_VERSION bumped v9 → v10 to refresh cached recs.
- **Manufacturer alias resolver** (Decisions #148, #149): `lib/services/manufacturerAliasResolver.ts` is the canonical MFR-identity layer, reading two sources on cold cache (5-min TTL). **Atlas** (Chinese): `atlas_manufacturers.{name_display, name_en, name_zh, aliases[]}`. **Western**: `manufacturer_companies` + `manufacturer_aliases` two-table graph (uid, parent_uid self-ref, status enum, 15 `context_code` taxonomy). `resolveManufacturerAlias(input)` returns `{ canonical, slug, source, variants, companyUid?, lineage? }` via exact case-insensitive hit. Western adds parent-chain walking: steps up through `parent_uid`, falling back to `context=acquired_by|merged_into` alias rows (source uses both mechanisms — user-confirmed intentional). Walk terminates at self-ref corporate/active entity or MAX_PARENT_HOPS=6. Variants for a canonical include every descendant's name + aliases. Collision policy: corporate/active canonical wins; ties broken by lowest uid. Atlas wins cross-source collisions. Wired into Atlas search (`atlasClient.searchAtlasProducts` dual-query), AddPart mismatch (`search-quick/route.ts`), BOM dedup (`usePartsListState` via client-safe `manufacturerAliasClient.ts` + `POST /api/manufacturer-aliases/canonicalize`), admin manufacturer aggregation (list + per-slug products), **BOM batch validation** (Decision #150: `lib/services/mfrMatchPicker.ts` → `pickMfrAwareMatch()` prefers the search candidate whose MFR canonically matches the user's input over blind `matches[0]` when multiple candidates exist, called from `app/api/parts-list/validate/route.ts`), **matching-engine preferred-MFR sort** (Decision #151: `isPreferredManufacturer()` accepts optional `manufacturerSlugLookup` for canonical-slug comparison; `partDataService.getRecommendations()` pre-resolves preferred + candidate MFRs so "prefer Analog Devices" floats Linear Tech / Maxim / Hittite parts too), and **admin alias editor** (Decision #152: dedicated "Aliases" tab on `/admin/manufacturers/[slug]` — sibling of Products / Flagged / Coverage / Cross-Refs / Profile. Click × on a chip to remove, type + Enter to add, optimistic PATCH → resolver cache auto-invalidates on save. `normalizeAliasInput()` helper exported from the PATCH route validates/dedupes/trims input — max 50 aliases, 100 chars each). Null = no match, callers fall through to existing substring behavior. Cache invalidated from `PATCH /api/admin/atlas/manufacturers` on toggle. Western deployment: apply `scripts/supabase-manufacturer-companies-schema.sql` then `node scripts/manufacturer-companies-import.mjs`. Upcoming AVL/AML/Line-Card ingestion will store `companyUid` as the stable FK.
- **MFR identity — products link by NAME STRING, atlas_id is the spine** (Decision #225): **load-bearing fact** — `atlas_products` has NO manufacturer ID column; products link to manufacturers purely by the `manufacturer` name string (upsert key `(mpn, manufacturer)`). The admin MFRs route folds per-row product counts across `[name_display, name_en, name_zh, ...aliases]`. Two failure modes this produced: (1) **true duplicates** — same `name_en`+`name_zh` = one company imported twice (a 2nd import keyed `onConflict: 'atlas_id'`, so the same company under a different atlas_id inserted a shadow row). Fixed: `atlas-dedupe-manufacturers.mjs` removed 9 shadows; `atlas-manufacturers-import.mjs` gained a name-based guard (skip incoming record whose `(name_en|name_zh)` already exists under a different atlas_id). (2) **English-code collisions** — same `name_en`, different `name_zh` = genuinely different companies sharing a short code (HX=红星/恒佳兴, LX=灵星芯微/连欣科技). These are legitimate rows; the bug was that ingest's `cleanManufacturerName()` stripped the Chinese half ("HX 红星"→"HX"), so both companies' products keyed under the ambiguous "HX" and the fold credited both. Fixed two ways: `atlas-rekey-collision-products.mjs` re-keyed existing products to the full source name via `atlas_source_file` (deterministic — every product stores its source file; the file's `manufacturer.name` IS the unique identity); and `cleanManufacturerName()` is now **collision-aware** (`loadCollidingEnNames()` auto-discovers `name_en` shared by >1 MFR at ingest start, keeps the full name for those — applies on UI Proceed too since `runProceed` re-maps from source and the proceed route spawns the .mjs). **Admin MFRs page** now also shows an **Atlas ID** column (the unique per-row source identity) + **average coverage** in the summary line. **Durable fix (BACKLOG P2, Steps 2–3):** add `manufacturer_atlas_id` FK to `atlas_products` + backfill from `atlas_source_file` + aggregate by ID, retiring the name fold. The bridge fixes are forward-compatible (backfill keys off source file, not the string).
- **List Agent** (Decision #111): Per-list conversational agent (`listChat()` in orchestrator, `/api/list-chat` route). 9 tools: 3 read-only (`get_list_summary`, `query_list`, `get_row_detail`), 3 client-side view (`sort_list`, `filter_list`, `switch_view`), 3 write with confirmation (`delete_rows`, `refresh_rows`, `set_preferred`). Write tools intercepted server-side — return `PendingListAction` descriptor rendered as confirmation button via `InteractiveElement` type `'list-action'`. System prompt includes user context (reuses `buildUserContextSection()`) + list context (name, description, customer, status counts, manufacturers, families). UI: sticky footer bar (34px) + bottom-anchored 50vh drawer. Conversation is ephemeral (MVP).

## QC & Feedback System

The QC page (`/qc`) is a top-level admin-only route (sidebar icon: `RateReviewOutlinedIcon`). It is **feedback-first**: the primary section shows user-submitted feedback items for triage, not raw recommendation logs.

### Architecture

- **Feedback Tab** (primary): Lists all `qc_feedback` rows with status filters (Open/Reviewed/Resolved/Dismissed), search, and sort. Clicking a row opens a detail view that reconstructs the comparison table from the JSONB snapshot stored in `recommendation_log`, highlighting the specific rule the user flagged.
- **Logs Tab** (secondary): Lists raw `recommendation_log` entries with feedback indicators. Has **Export** (CSV/JSON) and **AI Analysis** capabilities.
- **Recommendation Logger** (`lib/services/recommendationLogger.ts`): Writes snapshots (source attributes, recommendations, context Q&A, overrides) to `recommendation_log` as JSONB. Must always be `await`ed — fire-and-forget silently drops entries.
- **Feedback types**: `FeedbackStage` = `rule_logic` | `qualifying_questions`. `FeedbackStatus` = `open` | `reviewed` | `resolved` | `dismissed`.
- **Comparison reconstruction**: The feedback detail view reuses the row-building algorithm from `ComparisonView.tsx` — builds rows from `sourceAttributes.parameters` + `matchDetails`, highlights the flagged rule with a yellow left border.

### Export & AI Analysis (Logs Tab)

- **Export** (`/api/admin/qc/export`): CSV (flat columns + top 3 recs) or JSON (full snapshots) download. Respects current filters, caps at 10K rows. Client uses `window.open(url)` for direct download.
- **AI Analysis** (`/api/admin/qc/analyze`): Server-side aggregation (`lib/services/qcAnalyzer.ts`) computes per-family/per-rule stats from JSONB snapshots, sends compact summary to Claude Sonnet 4.5 via streaming API. SSE events forward text deltas to `QcAnalysisDrawer` (right-side MUI Drawer with `ReactMarkdown` rendering).
- **Aggregation approach**: Raw snapshots are too large for Claude (~5-10K tokens each). `aggregateQcStats()` computes rule failure rates, missing attribute frequency, score distributions, and feedback correlations, then includes 3-5 representative examples. Total input ~3-5K tokens regardless of log count.

### QC Component Structure

`QcShell.tsx` (`components/qc/`) is the top-level orchestrator with settings toggle in the header and `QcSectionNav` for Feedback/Logs sections. Content components live in `components/admin/`:
- `QcFeedbackTab.tsx` → `QcFeedbackDetailView.tsx` (feedback flow)
- `QcLogsTab.tsx` → inline DetailView + Export/Analyze buttons (logs flow)
- `QcAnalysisDrawer.tsx` — AI analysis right-side drawer with streaming markdown
- Shared: `QcFeedbackCard.tsx`, `QcRecommendationSummary.tsx`, `qcConstants.ts`

## App Feedback (Collaborative)

Separate from QC feedback. App feedback is the general user-facing channel — bugs, ideas, anything not tied to a specific recommendation rule. Two-way thread between the submitting user and the platform admin (Rob). Decisions #162 (initial submission flow + attachments), #220 (two-way thread + comments + read state), #222 (Trello-style overlay UX + activity-first sort + delete + RLS fix).

**Data model:**
- `app_feedback` — original submission + status (`open`/`reviewed`/`wip`/`resolved`/`dismissed`; `wip` = Work In Progress, added June 9 2026, ordered between reviewed and resolved — migration `scripts/supabase-app-feedback-add-wip-status.sql` updates the CHECK constraint on existing tables) + JSONB `attachments` + `user_last_read_at` + `admin_last_read_at`. RLS: users SELECT/INSERT own, admin SELECT/UPDATE all, user can delete own, admin can delete any.
- `app_feedback_comments` — `(id, feedback_id, author_id, author_role IN ('user','admin'), body, created_at)`. Immutable (no UPDATE/DELETE-by-author policies). Cascades on parent delete.
- `mark_app_feedback_user_read(p_feedback_id UUID) RETURNS TIMESTAMPTZ` — `SECURITY DEFINER` function. The only way a non-admin can stamp `user_last_read_at` — RLS blocks direct UPDATE for users (intentional, so they can't tamper with status/notes). Both the GET-thread route and the comment-POST route call this RPC. See [[silent-update-under-rls]] memory.
- Storage bucket `app-feedback-attachments`: per-user folder prefix, signed-URL reads (1h TTL), user can delete own, admin can delete any.

**Surfaces (user side at `/feedback`):**
- `FeedbackShell` — full-width card list + "+ New Feedback" header button + 30s background poll (paused when tab hidden, refreshes on `visibilitychange`).
- `FeedbackList` — `<Paper>` card per item (NOT `<ButtonBase>` — adding the kebab inside revealed that button-in-button is invalid HTML; Paper carries `role="button"` instead). Activity-first sort (`hasUnread` rows on top, then `createdAt desc`). Unread cards get primary-color border + "NEW REPLY" label + bold preview.
- `FeedbackDetailModal` (shared admin/user) — `<Dialog maxWidth="lg" fullWidth height: 90vh>` with a 60/40 flex body. Mounts → fires GET-thread → server stamps the appropriate `*_last_read_at` → modal echoes `onUpdated({})` so the parent list can flip `hasUnread` locally and clear the visual signals immediately.
- `FeedbackThread` (shared admin/user) — composer at top, comments below newest-first, avatar+name+timestamp+body feed layout (NOT chat bubbles). `viewerRole` prop only drives the "You" label.
- `FeedbackRowMenu` (shared) — kebab → "Delete thread" → confirm dialog → `DELETE /api/app-feedback/[id]` (cleans storage paths first, then DB row; comments cascade). Confirm copy explicitly spells out that delete affects both sides.

**Surfaces (admin side, `/monitoring → AppFeedbackTab`):**
- Table-based list (paginated). Default `sort_by=activity` — server fetches the full filtered set, sorts `(pinned desc, hasUnread desc, created_at desc)`, slices to the page. Column-header sort overrides activity until refresh.
- **Pin to top** (leftmost column, `PushPinIcon`): admin can pin feedback rows so they float to the top, mirroring the Atlas domain-cards pin. Pinned IDs are **per-browser localStorage** (`app-feedback-pinned-v1`) — no DB column, no API. The client (a) stable-partitions the rendered page pinned-first for instant feedback + non-activity sorts, and (b) sends `pinned_ids` to `GET /api/admin/app-feedback` so the server's activity sort floats pinned rows above the page slice — guaranteeing a pinned item surfaces on page 1 even past the 50-row page boundary. A `pinnedRef` feeds the current set into `load()` without re-creating the callback (no skeleton flash on toggle).
- Same `FeedbackDetailModal` mounts on row-click; admin-only path shows status dropdown + technical-info collapse + resolved-by metadata on the left column.
- Per-row "NEW" pill (no header) when `admin_last_read_at IS NULL AND status='open'`.
- "New feedback" button (left of the search bar) reuses the shared `AppFeedbackDialog` so the admin can author a feedback item directly (e.g. logging a bug they found or transcribing feedback received elsewhere). Created under the admin's own `user_id`; on submit, the list resets to page 0 and reloads so it surfaces at top.
- **No background poll on the table** (removed June 16, 2026 — the 30s re-fetch flashed the loading skeleton and read as "constantly refreshing"). The list refreshes only on user action: filter/sort/pagination, thread open/close, delete/create, and the `feedback-unread-changed` window event (action-driven, same-tab only) — plus a manual page reload for fresh server data. Freshness of the **needs-attention dot is unaffected**: the sidebar keeps its own lightweight `getAdminAppFeedbackList({ limit: 1 })` poll (count only, no table/skeleton) in [AppSidebar.tsx](components/AppSidebar.tsx), which runs on app load + every 30s, so new feedback still lights the nav dot without the user being in the section.

**Badge math (`needsAttentionCount`):** Both the Monitoring sidebar dot AND the App Feedback nav-section dot drive off a single server-computed field returned on the admin list response. Definition: count of rows where `hasUnread=true` OR (`admin_last_read_at IS NULL AND status='open'`). Status state alone does NOT light the dot — opening the modal (which stamps the read column) clears it. The user-side feedback icon (`ForumOutlinedIcon`, bottom-left of the sidebar above Releases) has its own `getOwnAppFeedbackUnreadCount` endpoint, same semantics from the user's perspective.

**Endpoints** (under `app/api/`):
- `POST /api/app-feedback` — submit (multipart, attachments)
- `GET /api/app-feedback` — own list, includes `hasUnread`/`commentCount` per row
- `GET /api/app-feedback/[id]` — own thread, side-effect stamps `user_last_read_at` via RPC
- `DELETE /api/app-feedback/[id]` — wipe thread (storage + row); RLS gates user-own / admin-any
- `POST /api/app-feedback/[id]/comments` — append comment; role inferred from `profiles.role`; stamps author's own read
- `GET /api/app-feedback/unread-count` — sidebar dot driver for user side
- `GET /api/admin/app-feedback` — list + `statusCounts` + `needsAttentionCount`; supports `sort_by=activity` + `pinned_ids` (comma-separated, floats pinned rows above the page slice)
- `PATCH /api/admin/app-feedback/[id]` — status only (admin_notes removed; comments replace it)
- `GET /api/admin/app-feedback/[id]/thread` — admin variant of thread fetch; stamps `admin_last_read_at`

## Data-Source Integrations (Digikey · Parts.io · FindChips · Mouser)

- **Digikey** — primary parametric source. `digikeyClient.ts` (OAuth2), `digikeyMapper.ts`, `digikeyParamMap.ts` (maps `ParameterText` → internal `attributeId`). Discovery script: `scripts/discover-digikey-params.mjs`.
- **Parts.io** — gap-fill enrichment only; **Digikey values always win**. `partsioClient.ts` / `partsioMapper.ts` / `partsioParamMap.ts`.
- **FindChips** — multi-distributor pricing/stock/lifecycle (~80 distributors, one call). `findchipsClient.ts` / `findchipsMapper.ts`. **Price and stock are never L2-cached** (Decision #158).
- **Mouser** — retained ONLY for `SuggestedReplacement` lookups (Decision #97); no longer a search or commercial source.

**Full detail — clients, mappers, param maps, caches, rate limits, taxonomy surprises, env vars and the certified-cross model — is in [docs/DATA_SOURCES.md](docs/DATA_SOURCES.md). Read the relevant section before touching a source's client, mapper or param map.**

## Replacement Preferences (Decisions #145, #146)

Per-list configuration stored on `parts_lists.replacement_priorities` JSONB column, shape `ReplacementPriorities` in `lib/types.ts`. Editable via List Settings → Replacement Preferences tab (`ReplacementPrioritiesField.tsx`). Two kinds of inputs:

- **Scoring inputs** (affect composite score + cache variant): `order: ReplacementAxis[]` (priority ranking of Lifecycle / Compliance / Cost / Stock), `enabled: Record<ReplacementAxis, boolean>`. Changes trigger `handleRefreshRows`. Composite score computed in `getRecommendations()` via `computeCompositeScore()` (lib/services/compositeScore.ts), stored on `XrefRecommendation.compositeScore`. Used as within-bucket tiebreak: match % floor for Logic within ±2%, composite primary for certified buckets. Stock axis is gated — only active when source `totalStock < 100`.
- **Display filters** (client-side, not cache-varied): `hideZeroStock?: boolean`, `maxReplacements?: 1-5`, `buckets?: RecommendationBucket[]`, plus **`preferBuyable?: boolean` + `buyableRequires?: 'price' | 'price_and_stock'` (Decision #260)**. Applied in `PartsListTable.tsx` via `pickEffectiveTopRec()` + `recPassesFilters()` + `getAlternateReplacements()`. No refresh triggered on toggle — `handleUpdateListDetails` separates `rankingChanged` (only `order`/`enabled`) from filter-only changes. Legacy `suggestionBuckets` / `maxSuggestions` keys are still accepted on read (pre-Decision-#147) and progressively migrate on next save.
- **Prefer-buyable #1 pick** (Decision #260, default ON): when the best-matching #1 replacement has no live distributor availability, `pickEffectiveTopRec()` promotes the closest *buyable* candidate (the demoted certified cross stays visible as an alternate). "Buyable" = has a distributor price (`'price'`) or price **and** stock (`'price_and_stock'`, default), per `isBuyable()` in `lib/services/buyableSelection.ts` (pure, unit-tested). Reach is the row's persisted candidate pool — `replacement` + alternates + `cheapestViableRecs` (the last is FC-enriched in `runFCEnrichment` when the pref is on, near-free since MPNs overlap). Display-only, instantly reversible, no cache bump; never hides (a row with no buyable cross keeps its pick) and never overrides an explicit `preferredMpn`.
- **🇨🇳 flag on Chinese (Atlas) MFRs in the list table** (Decision #261): `MfrWithFlag` helper in `PartsListTable.tsx` appends the same flag+tooltip as the recommendation cards. Repl. MFR keys off `topRec.part.mfrOrigin === 'atlas'` (covers alternates); source MFR (`mapped:manufacturer`/`dk:manufacturer`) keys off `row.resolvedPart?.dataSource === 'atlas'` (mfrOrigin isn't resolved for source rows in the list flow).

**Three-layer defense for `hideZeroStock`:** (1) render-time swap picks first stocked candidate from persisted `[replacement, ...replacementAlternates]`; (2) post-FC-enrichment promotion effect persists the swap; (3) deep-fetch effect (concurrency 2) handles rows where all persisted top-3 are zero-stock — calls `getRecommendations` + `enrichWithFCBatch(top-30)` because `getRecommendations` returns candidates with empty `supplierQuotes` by design (FC enrichment deferred). `deepFetchAttemptedRef` prevents infinite retry on cohorts with no stocked alternative.

**Persisted alternates cap:** bumped 2 → 4 in `toStoredRows` (both Supabase + localStorage) to support `maxReplacements: 5`. Existing lists progressively migrate on next write.

**Repl. Distributor column (`sys:top_suggestion_supplier`):** reads `supplierQuotes[0].supplier` (pre-sorted by best unit price). Uses `SUPPLIER_DISPLAY` from `AttributesTabContent.tsx`. Column ID preserved pre-rename; header label is "Repl. Distributor".

**Vocabulary (Decision #147):** Parts-list code uses "replacement" throughout. `PartsListRow.replacement` = the top proposed swap (singular); `PartsListRow.replacementAlternates` = positions #2–#5. Storage layer reads legacy `suggestedReplacement` / `topNonFailingRecs` keys for back-compat. Do NOT confuse with `LifecycleInfo.suggestedReplacement` — that's Mouser's manufacturer-published successor MPN on EOL parts, a genuinely different concept.

**Column picker MPN vs SKU:** `dk:mpn` (Product Identity, label "MPN (DK)") surfaces `EnrichedPartData.mpn` — canonical MPN as resolved by source, distinct from user's raw `mapped:mpn`. `dk:digikeyPartNumber` renamed to "DigiKey SKU" and moved to Commercial group — it's a distributor SKU, not part identity.

## Product Direction

The app is evolving from a cross-reference tool into a component intelligence platform built on five pillars:

1. **Technical Matching** (built) — Deterministic rule engine across 43 families
2. **Commercial Intelligence** (planned) — Multi-supplier pricing, availability, lead times
3. **Compliance & Lifecycle** (planned) — EOL tracking, environmental/trade compliance, qualifications
4. **Data Integration** (planned) — Unifying distributor APIs, Atlas (Chinese MFR dataset), customer data, and market feeds
5. **Supply Chain Intelligence** (future) — Geopolitical risk, market monitoring, proactive alerts

### Data Sources

Digikey (primary, built) · Parts.io (gap-fill, built) · Atlas (Chinese MFR dataset, built) · FindChips (multi-distributor commercial, built) · Mouser (retained for `SuggestedReplacement` only) · Customer data (planned).

**Full detail — every source's scope, quirks, env vars, tables and scripts — is in [docs/DATA_SOURCES.md](docs/DATA_SOURCES.md). Read it before touching any data source.** Chinese-manufacturer options are surfaced with a subtle flag in recommendations (informative, never promotional).

### User Context Model

Three levels, more specific overriding more general: **User Profile** (`profiles.preferences` JSONB) → **List Context** (`parts_lists.context` JSONB) → **Per-Search Context** (family application-context questions). Global prefs become `AttributeEffect[]` via `contextResolver.ts`, reusing the existing context-modifier system — the matching engine itself does not change. Detail in [docs/DATA_SOURCES.md](docs/DATA_SOURCES.md); vision and phasing in [docs/PRODUCT_ROADMAP.md](docs/PRODUCT_ROADMAP.md).

## Running

```bash
npm run dev     # Start dev server (Turbopack)
npm run build   # Production build
npm run lint    # ESLint
npm test        # Jest unit tests (3511 tests, ~2s)
npm run test:watch  # Jest in watch mode
npm run verify  # tests + the quality ratchet — what CI runs
npm run baseline        # ratchet only: fails if lint/type error counts RISE
npm run baseline:update # lock in a genuine reduction
npm run atlas:golden    # regenerate the ingest-mapper golden file, then READ the diff
```

**Testing routes (Decision #277).** Until July 2026 this repo had 1,631 tests and **not one executed an API route** — while every write to a parameter mapping happens in a route. There is now a harness in [`__tests__/helpers/`](__tests__/helpers/) (zero new dependencies, `testEnvironment: 'node'`, no jsdom): `supabaseMock.ts` (stateful, supports **writes**, records filters and op order, injects per-table/per-op errors), `authGuard.ts`, `routeHarness.ts`. Two traps: `jest.mock()` paths must be **RELATIVE** (`../../lib/...`; the `@/` alias resolves for `import` but not inside `jest.mock`), and `lib/supabase/server` + `lib/supabase/service` must be pointed at **one shared instance via `globalThis`** or a cross-table assertion is two unrelated hopes. A write returns `data: null` unless `.select()` is chained — the mock models that deliberately. The mock does NOT model unique indexes, RLS, FK/CHECK, Postgres collation, the 1000-row cap, or concurrency; those belong to [docs/QA_PARAM_MAPPING.md](docs/QA_PARAM_MAPPING.md).

**The bar for these suites is mutation, not green.** Break the source one line at a time and the suite must catch it. Every mapping-write route, the override read layer and the real ingest mapper were built this way; it repeatedly found gaps review had not (documented contracts nothing enforced, a filter the stub silently ignored, thin fixtures). A suite that only passes proves nothing — see the `green-test-must-fail-on-broken-code` memory.

**`scripts/atlas-ingest.mjs` is importable but still a standalone CLI.** An entrypoint guard (`IS_CLI`) means importing it does not parse argv, build a Supabase client, or run the dispatcher; it exports `mapModel` / `classifyAtlasCategory` / `cleanManufacturerName` **for tests only**. Any change here must keep `--report --dry-run` and the no-argument usage output byte-identical (stdout, stderr, exit code). The golden file pins the mapper's real output over real fixture models: regenerate with `npm run atlas:golden` **and read the diff** — it names, in attribute names, exactly what your change did to product data.

**No parameter value is ever discarded** (Decision #278). When two source spellings resolve to one `(family, attributeId)` slot the first wins — but the loser is **kept under its raw parameter name** via `storeRawValue()`, never dropped. This is not a nicety: accepting a mapping in Triage used to *delete* the value it displaced (a weight-9 `rds_on` rule was left holding a *typical* number where the *maximum* had been available), and 534,819 values across 437,093 products were being binned this way. Three details are load-bearing and each is pinned by a mutation-tested case: the raw key **keeps Unicode letters/digits** (an ASCII-only slug reduces a pure-Chinese name to `''` and drops the value — that alone was 251,643 values, and it also gave Triage an empty `attributeId`); **identical** values are still dropped (nothing to preserve, and the comparison is deliberately conservative — a false "differs" costs a redundant copy, a false "same" deletes data); and colliding keys are **suffixed, never overwritten** (`MAX_LOSER_SUFFIX = 200` is a runaway guard, *not* a data cap — a bound of 20 dropped 1,530 real values). The first occurrence keeps its historical key, so no ingested row re-keys **unless it collides with a scoring slot** (see below). ⚠️ The rule exists in **both** copies and they are pinned against each other by the parity block in [__tests__/services/atlasLayer1KeepLoser.test.ts](__tests__/services/atlasLayer1KeepLoser.test.ts) — a "keep in lockstep" comment is not a mechanism, and note the parity block must compare **KEYS as well as values**: a normalized winner (`"<30 V"` → `"30"`) and a rescued loser (`"30"`) produce an identical value set whether the loser was preserved or deleted. Layer 1 stops a wrong mapping *destroying* data; it does not make the mapping *right* — several semantically wrong mappings it exposed (power → `color`, current → `supply_voltage`) are in BACKLOG.

**An unrecognised parameter's number must still be scaled — but ONLY when its unit is certain** (Decision #280, supersedes the storage half of #279). `matchingEngine.ts:23` returns a stored `numericValue` **verbatim, with no re-parse and no unit check**, and the rescued path (`storeRawValue`, for a column no dictionary recognises) used bare `extractNumeric` — it read the number and ignored the unit. Siliup/SP40N25TQ ships `导通电阻(RDS(on)) = "80mΩ@10V"`, which slugs onto `rds_on` (weight 9, `lte`) and was stored as **80, not 0.08**. Demonstrated end-to-end: a genuinely better 80 mΩ candidate hard-*failed* against a 100 mΩ source; it now passes rated better. Measured over all 429 files / 437,387 products: **153,993 values corrected, 50,521 of them in scoring slots, across 69,639 products.** The fix is ONE line in `storeRawValue` in both mapper copies — `rescueNumericValue`, which scales **only** when the unit is on a conservative allowlist shared verbatim via [lib/services/atlas-rescue-units.json](lib/services/atlas-rescue-units.json) (`{p n u µ m k K M G} × `{F A V W H Hz s Ω J C S}`, minus `MS`/`KS`). ⚠️ **The allowlist IS the safety property.** Both "proper" fixes were measured and rejected: a first-letter rule reads `ppm` as pico (11,122 values) and `pcs` as pico (11,254); a strict whole-token rule instead BREAKS `PF` (1,989), `nV/√Hz` (474), `mW/sr` (391), `Mbit` (431). There are **367 distinct unit spellings** here — recognising them all is its own project. An unrecognised token keeps today's value EXACTLY, so this can only make a number more correct or leave it alone; **it can never introduce a wrong one.** Uppercase `P`/`U`/`N` are absent for the same reason (enabling `PF` generatively also enables `UV`/`PW`). **Scope: the rescued path only** — the dictionary-mapped paths already normalise and are untouched. Four gates ran over the full corpus and must stay at zero for any change here: **keys added / keys removed / display strings changed / numbers lost.**

**#279's reserved-id RENAME was withdrawn — do not reintroduce it** (Decision #280). #279 moved an unvetted value to a new key (`rds_on` → `raw_rds_on`) so it could not be scored. But **the key is a storage contract**: four consumers find values by it — the Specs panel (`fromParametersJsonb`), the coverage RPC, the widening RPC (in SQL, where `raw_` has no meaning) and the reclassify RPC — and only two were updated. Measured: the rename changed **14,110 key names across 6,559 products (16%)**, dropped coverage corpus-wide, leaked `raw_` labels to users, and freed the old slot so a *different* measurement could occupy it (verified live: YANGJIE/FRD60A600AS-290A, vendor `VRRM=600` displaced by dict-mapped `VR (V)=630`). It also demoted CATEGORICALS (`channel_type = "N"`), which makes matching **more permissive, not safer** — a missing value scores `review` = 50% credit that never fails, so an N-channel source could surface P-channel parts. ⚠️ An `unvetted: true` annotation was designed as the replacement and **also rejected**, on two verified grounds: `fromParametersJsonb` builds its output as an explicit field-by-field literal, so an added field is silently dropped and never reaches the engine; and the mapped write is an *unconditional* overwrite, so a naive annotation would have **destroyed** the value the rename at least preserved. The real lesson: **the wrong-scale number was the whole bug** — an unlabelled column named `RDS(on)` landing in `rds_on` is usually correct, and once its scale is right it scores correctly.

Requires `.env.local` with: `ANTHROPIC_API_KEY`, `DIGIKEY_CLIENT_ID`, `DIGIKEY_CLIENT_SECRET`, `PARTSIO_API_KEY`, `MOUSER_API_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `REGISTRATION_CODE`.

Optional: `XREFS_API_KEYS` (comma-separated Bearer tokens for external API access), `SUPABASE_SERVICE_ROLE_KEY` (for MCP server Supabase access).

## External API Access (Decision #80)

The matching engine is accessible to sister products and external systems via two paths:

**REST API** — Existing endpoints (`/api/search`, `/api/attributes/{mpn}`, `/api/xref/{mpn}`) accept `Authorization: Bearer <key>` for machine-to-machine auth. Keys configured in `XREFS_API_KEYS` env var. See `docs/API_INTEGRATION_GUIDE.md` for full documentation.

**MCP Server** — Standalone stdio-transport server for AI agent integration. 5 tools: `search_parts`, `get_part_attributes`, `find_replacements`, `list_supported_families`, `get_context_questions`. Run with `npm run mcp:dev`. Test with `npm run mcp:inspect`. See `mcp-server/` directory.

**Auth architecture:** `requireAuth()` in `lib/supabase/auth-guard.ts` checks API key first, then falls back to Supabase cookie auth. API key users get a fixed service user ID; admin routes remain blocked. `lib/supabase/server.ts` has a try/catch fallback that returns a direct Supabase client when `cookies()` from `next/headers` is unavailable (MCP server, standalone scripts).
