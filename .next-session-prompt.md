# Next-session prompt — Domain card auto-audit Phase 2

Copy everything below the `---` line into the chat to start the next session.

---

Continuing from yesterday (May 21, 2026). Production-bound work — this app will be in front of real customers soon, so no shortcuts and no quick-fix mentality.

**GOAL:** Ship Decision #195 Phase 2 — wire the family-domain-card audit into the Generate flow so it runs automatically, persists results, surfaces them in the UI, and (last) gates Activate when critical hallucinations are present.

**Background to read before proposing the plan:**
- `docs/DECISIONS.md` #195 — Phase 1 (shipped May 21) + the four hallucination classes the audit catches + the AI-content-trust pattern
- `docs/BACKLOG.md` entry "Domain card auto-audit on generation + Activate gating (Decision #195 Phase 2)" — full scope with recommended ordering and open questions to resolve
- `scripts/atlas-audit-domain-cards.mjs` — the CLI script whose logic we're extracting to a TS service
- `app/api/admin/atlas/family-domain-cards/[familyId]/generate/route.ts` — the Generate endpoint we'll hook into
- `app/api/admin/atlas/family-domain-cards/[familyId]/route.ts` — the Activate workflow we'll add gating to (the riskiest piece)
- `components/admin/AtlasDomainCardsPanel.tsx` — where the audit-results UI panel will live
- `scripts/supabase-atlas-family-domain-cards-schema.sql` — existing schema we'll add an `audit_results JSONB` column to

**Recommended sequencing (from BACKLOG, six pieces — additive first, gate-modification last):**
1. Schema migration: add `audit_results JSONB` column to `atlas_family_domain_cards`. Idempotent `ADD COLUMN IF NOT EXISTS`. I'll show you the SQL; you apply via Supabase SQL Editor manually (no service-role schema changes from scripts).
2. Extract audit to TS service module `lib/services/atlasFamilyCardAudit.ts`. Type-check.
3. Hook into Generate endpoint — audit runs after Anthropic returns; persists to `audit_results`. Verify by triggering a fresh Generate.
4. UI panel in `AtlasDomainCardsPanel.tsx` (or wherever cards are reviewed) showing audit summary inline. Default-expanded. Verify in dev.
5. Manual "Re-run audit" button + new route `POST /api/admin/atlas/family-domain-cards/[familyId]/audit`. Verify.
6. **Activate gating** — modifies the existing Activate workflow. Highest-risk piece. Propose the diff before applying it. After it ships, run `/ultrareview` before final commit.

**Constraints:**
- Solo dev, no human reviewer — extra discipline on Piece 6 specifically. Plan-before-code; show me the diff before applying.
- DO NOT silently block Activate — when audit-severity exceeds threshold, show a warning + require typed confirmation ("I understand this card has N flagged issues"). Engineer must always be able to override with intent. Decision #195 lesson: gating, not silent blocking.
- Audit failure must NOT block Generate — if the audit throws, persist the card with `audit_results: { error: '...' }` and move on. The audit is a safety net, not a critical path.
- If audit-severity threshold needs to be configurable, defer that until we see real-card data — start hardcoded.
- Solo-dev discipline: any meaningful code change runs `npx tsc --noEmit` before commit; for SQL migrations and workflow-gate logic, propose plan + get approval before coding.

**Open questions to resolve during the session (from BACKLOG):**
- Should `audit_results` invalidate automatically when atlas_products changes (any ingest apply)? Or only on manual re-run? Trade-off: stale audits vs operational cost of re-auditing all cards on every batch apply.
- Severity threshold default — start at 1 (any non-omission issue blocks)? Or 3 (multiple issues)?
- Does Activate gating apply to BOTH new card activations AND re-activations after Regenerate? (Probably yes — same risk surface.)

**Definition of done:**
- Generate any draft card → audit runs automatically → results visible in UI without me clicking anything
- Manual re-run button works
- Activate on a dirty card shows the warning + typed-confirmation gate; can be overridden by typing the exact phrase
- Decisions #195 Phase 2 status updated to RESOLVED; BACKLOG entry marked complete
- `/ultrareview` passed before final commit
- CLAUDE.md and memory updated as needed

**Starting move:**
Read the context files above, propose concrete plan for Pieces 1–3 (the additive, lower-risk first batch), get approval, ship them, checkpoint with me before moving to Pieces 4–5, then propose Piece 6 diff separately for approval.
